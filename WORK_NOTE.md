# WORK NOTE — 뉴스 피드 프로젝트

## 2026-08-05 — 실현 가능성 확인 및 계획 수립

- 요구사항 정리: GitHub public + 웹서비스, iPhone 에서 확인, 카테고리 선택, 언론사/피드 차단, 카테고리별 정렬, 대시보드(중요 뉴스 모음), 날씨.
- 실현 가능성 실측:
  - Google 뉴스 RSS (ko-KR 전체 + TECHNOLOGY topic 피드) 정상 응답 확인 — 항목별 `<source>` 태그 존재 (언론사 차단 가능 근거).
  - Open-Meteo forecast API 정상 응답 확인 (서울, 현재+주간, 키 불필요).
  - 한겨레·매경 개별 RSS 는 이 세션 fetch 제한으로 미검증 → M8 로 이연.
- 아키텍처 결정: GitHub Actions cron 수집 → `news.json` → GitHub Pages 정적 PWA. 서버 없음.
- [PLAN.md](PLAN.md) 작성 (아키텍처·기능 설계·M1~M8 마일스톤·한계), [AGENTS.md](AGENTS.md) 에 TODO 정리.
- 구현은 미착수 (계획 승인 대기).
- 추가 요구 반영: 외국 뉴스 번역(수단 선택 대기), 대시보드를 위젯 방식으로 변경(추가/삭제/순서변경 + 위젯별 설정).
- 추가 API 실측: Open-Meteo Air Quality(미세먼지 PM10/PM2.5) ✅, Frankfurter 환율 ✅ — 둘 다 키 불필요. 주가지수는 무료 공식 API 없어 보류.
- 바로가기 링크 위젯을 초기 위젯에 추가 (링크 추가/삭제/편집, iPhone URL scheme 지원).
- UI 와이어프레임 작성(대시보드/뉴스/바로가기 편집 + 화면 흐름도) → 주인님 승인, PLAN.md 에 확정 반영. https://claude.ai/code/artifact/fb51d19f-a963-451f-b8a3-286f11d0067d

## 2026-08-05 — M1 수집 파이프라인 + M2 기본 뷰어 구현

- **M1 완료**: [scripts/fetch-news.mjs](scripts/fetch-news.mjs) — 의존성 0 (Node 내장 fetch). Google 뉴스 RSS 9개 카테고리(주요/정치/경제/IT·과학/세계/사회/스포츠/연예/건강) 수집 → 제목 토큰 Jaccard 유사도(≥0.5) 클러스터링 → coverage(관련보도 수) × 최신성(반감기 12h) 스코어 → `site/data/news.json`. 로컬 실행 9/9 카테고리 성공 (총 416건).
  - 정치 카테고리만 topic 피드가 없어 검색 RSS(`정치 when:1d`) 사용.
- **M1 완료**: [.github/workflows/collect.yml](.github/workflows/collect.yml) — 30분 cron + push + 수동 트리거 → 수집 → Pages 배포. **push 전이라 실동작 미검증.**
- **M2 코드 완료**: [site/index.html](site/index.html) + css/js — 하단 탭 3개(대시보드·뉴스·설정, 대시보드/설정은 placeholder), 카테고리 칩, 기사 리스트(제목→원문 새 탭, 언론사·상대시간·"n곳 보도" 배지). 로컬 서버(http-server)+Edge headless 390×844 스크린샷으로 렌더링 검증 완료.
- **관찰 (M5 때 개선)**: Google 뉴스 description 의 관련기사 리스트가 최대 5개라 coverage 가 5 로 포화되는 기사가 많음 → 중요도 변별력을 높이려면 카테고리 간 교차 클러스터링 또는 배지 표시 기준 상향 필요.
- 남은 것: GitHub repo 생성·push·Pages 활성화 (주인님 지시 대기) → iPhone 실기 확인.

## 2026-08-05 — M3·M4·M5 구현 (커밋 9cf4e0d)

- **M3 차단**: 기사 ⋯ → 액션 시트에서 언론사 차단 / 키워드 차단. 설정 탭에 차단 목록(칩 + ✕ 해제) + 직접 추가 입력(언론사는 수집 데이터 기반 자동완성 `datalist`).
- **M4 정렬·카테고리**: 뉴스 상단 "정렬: ○○ ▾" → 최신순/중요도순/언론사 우선. 카테고리별로 따로 저장. 설정 탭에서 카테고리 체크박스 on/off + △▽ 순서변경, 선호 언론사 순위 관리.
- **M5 위젯 대시보드**: `WIDGET_TYPES` 레지스트리 + 공통 `widgetFrame()`(⋯ → 위로/아래로/삭제) + ＋위젯 추가. 중요뉴스 위젯(건수 3/5/10, 카테고리 배지) / 바로가기 위젯(전체화면 편집 패널: 추가·편집·삭제·순서변경).
  - 중요도 포화 문제 대응: 카테고리 교차 풀에서 스코어 정렬 후 제목 유사도 0.6 이상 중복 제거, "n곳 보도" 배지는 5 이상만 표시.
- 탭 상태를 `?tab=` / `#hash` 로 지정 가능 (헤드리스 캡처가 `#` 를 별도 타겟으로 오해해 쿼리 방식 추가).
- **검증**: 로컬 서버 + Edge headless 390px 스크린샷 3장(대시보드/뉴스/설정) + DOM 진단 페이지. 진단 결과 `scrollWidth==clientWidth==390`(가로 오버플로 없음), 링크 4/4 렌더, ⋯ 38개·위젯 버튼 visible. **스크린샷에서 우측이 잘려 보이는 것은 Edge headless 캡처 특성이며 실제 레이아웃 문제 아님.**
- 미리보기 아티팩트(M5 반영): https://claude.ai/code/artifact/8ec90382-e44c-4052-8db6-dc41b8fc7e10

## 2026-08-05 — 배포 완료 + M6·M7

- **배포**: GitHub `jayhouse2001/news_feed` (branch `master`). Pages Source = **GitHub Actions** 로 설정 → https://jayhouse2001.github.io/news_feed/ 로 서비스 중, iPhone 확인 완료.
  - 브랜치명은 `main` → `master` 로 변경. 원격 `main` 삭제는 GitHub 기본 브랜치를 master 로 바꾼 뒤에야 가능(그 전엔 `refusing to delete the current branch` 로 거부됨).
  - workflow trigger 도 `master` 로 수정 (안 고치면 push 해도 갱신 안 됨).
  - ⚠️ `index.html` 은 `site/` 안에 그대로 둔다. Pages Source 가 GitHub Actions 이므로 `site/` 가 사이트 루트로 배포됨 — 루트로 옮길 필요 없음(옮기려다 원복함).
- **M6 날씨·미세먼지 위젯**: Open-Meteo forecast + air-quality 를 브라우저에서 직접 호출(키 불필요·CORS 허용). 한 위젯에 기온·날씨·최고/최저 + PM2.5 등급 칩(좋음≤15/보통≤35/나쁨≤75/매우나쁨)·PM10·강수확률. 위젯 헤더 버튼으로 지역 선택(프리셋 7곳 + geolocation). 좌표 키 기준 캐시.
- **M7 PWA**: manifest.json + icons(192/512, maskable 포함) + iOS 메타 태그. 아이콘은 SVG 를 Edge headless 로 PNG 렌더해 생성(SVG 원본은 삭제).
- **검증**: 로컬 서버 + Edge headless. 날씨 위젯 실데이터 렌더 확인(31°, 맑음, PM2.5 17 "보통", 강수 82%), manifest·아이콘 3종 HTTP 200, iOS 메타 태그 4종 존재, 가로 오버플로 없음.

## 2026-08-05 — UI 전면 개편 (좌우 스와이프 페이저)

주인님 지시로 하단 탭 구조 폐기. [PLAN.md](PLAN.md) "UI 구성" 절 개정.

- **페이저**: `body` 를 flex 컬럼 + `overflow:hidden` 으로 고정하고, `.pager` 를 `scroll-snap-type: x mandatory` 로 좌우 스와이프. 페이지 = 대시보드 1개 + 활성 카테고리 N개 (스크롤은 페이지 안에서 세로로).
  - 제목/인디케이터는 `scroll` 이벤트에서 `scrollLeft / clientWidth` 로 현재 인덱스 계산해 동기화(rAF 스로틀).
- **하단 탭바 제거**, 설정은 헤더 오른쪽 ⚙ 아이콘 → 전체화면 패널.
- **헤더 아래 칩 줄**: 페이지 인디케이터 + 탭하면 해당 페이지로 이동.
- **날씨**: `geolocation.getCurrentPosition` 으로 **휴대폰 위치 자동 취득**(8초 타임아웃, 10분 캐시) → 그 좌표의 현재 날씨 + **7일 예보**(요일별 아이콘·최고·최저). 실패 시 저장된 위치 → 서울 폴백. 지역 프리셋 목록은 제거.
- **검증**: DOM 진단 — 탭바 없음 확인, 페이지 11개(대시보드+카테고리 10), pager scrollWidth/clientWidth = 11.0, snap type `x mandatory`, 주간 셀 7개(오늘 목 금 토 일 월 화), 스와이프 후 제목·활성 칩이 "주요"로 전환, 세로/가로 오버플로 없음. 실화면 스크린샷으로 레이아웃 확인.

## 2026-08-05 — 마무리 (해외 번역 · 차단 UI · 열기 방식)

커밋: `173998c` → `2ed0845` → `415627d` → `e06e3cc` → `620ac43` (전부 push 완료)

- **새로고침 버튼**: 제목 오른쪽. `news.json` 재요청 → 페이지 재생성. 로딩 중 SVG 회전 + 중복 클릭 차단, 실패 시 "업데이트 실패" 2초 후 복구.
- **아이콘**: 설정 톱니바퀴를 방사형 선(태양처럼 보였음) → 실제 톱니 6개 형태로 교체. 앱 아이콘은 iOS 용 `apple-touch-icon.png`(180) 추가 — **로컬에만 있고 push 안 되어 홈 화면에서 안 보였던 것**이 원인이었음.
- **기사 열기 방식** (설정 최상단, 3택): 현재 창(뒤로 시 페이지·스크롤 복원) / 새 창 / 앱 안에서 읽기(리더 + "◀ 목록").
  - ⚠️ reader 모드는 Google 뉴스가 iframe 삽입을 거부해 실사용 불가 수준. Safari 폴백 버튼으로 대응 중. 개선안은 AGENTS.md 남은 후보 참조.
- **해외 뉴스 번역**: US edition 8개 카테고리(해외/세계/경제/IT/과학/건강/스포츠/연예, 384건) 수집 + gtx 로 제목 번역. 검증 5/5 성공, 캐시 90건. 국내 기사는 미변경. 작업 허브 `widget_news.js` 와 동일 방식.
- **언론사 차단 UI**: 점선 칩 그리드(탭 → 취소선), 계열 묶음 칩(전체 차단 ↔ 전체 해제, 일부만 차단 시 점선 표시), 개별 해제 가능. 기본 차단 19곳 = 조선·중앙·동아·국민 + 계열사 (**동아사이언스 제외 — 과학 기사 필요**).
- **고친 버그 3개**:
  1. 계열 칩이 렌더 시점 상태를 캡처해, 일부만 차단된 상태에서 누르면 전체 차단이 아니라 전체 해제됨 → 클릭 시점에 상태 재조회.
  2. `defaultBlocksApplied` 플래그를 병합된 객체에서 읽어 마이그레이션이 한 번도 실행되지 않음 → 기존 기기에 기본 차단이 적용 안 됨. 저장된 원본에서 읽도록 수정.
  3. 설정 변경 핸들러가 `openSettings()` 만 호출해 패널 뒤 대시보드·페이지가 갱신되지 않음 → `applySettingChange()` 로 통일.
- **현재 상태**: 19페이지(대시보드 + 카테고리 18), 기사 767건, 위젯 3종. https://jayhouse2001.github.io/news_feed/
- ⚠️ **미배포 — 막힌 지점**: 로컬 커밋 2개(1a4ad65, 9cf4e0d)만 존재.
  - `gh` CLI 미설치. `winget install GitHub.cli` 는 비대화형 세션에서 진행되지 않음(응답 없음, 미설치 확인).
  - Windows 자격증명에 `git:https://github.com` 항목은 존재하나 소유 계정 미확인. `git ls-remote https://github.com/jayhouse/news-feed.git` → "Repository not found" (인증 프롬프트는 없음).
  - `git credential fill` + GitHub API 로 계정·scope 확인 시도는 보안 분류기에 차단됨.
  - ⇒ 배포 재개에는 주인님의 GitHub 계정명 + (gh 설치 또는 토큰 사용) 승인 필요.

## 2026-08-05 — 뉴스 화면 당겨서 새로고침

- 각 뉴스 카테고리 화면이 맨 위일 때 아래로 당겼다 놓으면 `news.json`을 다시 불러오도록 구현.
- 세로 당김이 확정된 경우에만 동작해 기존 좌우 페이지 스와이프와 충돌하지 않으며, 대시보드에는 적용하지 않음.
- 당김 거리와 임계점(놓아서 새로고침), 업데이트 진행 상태를 화면에 표시.
- iOS에서 당김 표시의 높이 변경이 `scrollTop`을 움직여 제스처가 끊길 수 있어, 레이아웃에 영향을 주지 않는 `absolute` + `transform` 방식으로 수정. 로컬 브라우저에서 18개 뉴스 화면의 표시 영역, 초기 스크롤 위치, 비레이아웃 배치를 확인.

## 2026-08-05 — 날씨 위젯 현재 위치 갱신

- 날씨 위젯 헤더에 현재 위치 아이콘 버튼과 위치 표시를 배치.
- 버튼을 누르면 geolocation의 10분 캐시와 날씨 메모리 캐시를 무시하고 현재 좌표·날씨·미세먼지를 다시 불러오도록 구현.
- GPS 좌표를 BigDataCloud 무료 클라이언트 역지오코딩 API로 변환해 아이콘 옆에 실제 도시·동네명을 표시. 위치명 조회 실패 시 `현재 위치`로 폴백.

## 2026-08-06 — M9 하단 탭바 + 이슈 트래커 + 스크랩

변경: `site/index.html`, `site/css/style.css`, `site/js/app.js`.

### 하단 탭바 복원 + 계층 분리

초기(M2)에 있던 하단 탭이 `173998c`(스와이프 페이저 도입)에서 `pagedots` 로 흡수되면서, "대시보드"와 "정치"가 같은 급으로 한 줄에 섞여 있었음. 성격이 다른 것을 분리:

- **하단 탭바 = 섹션 전환** — 대시보드 · 뉴스 · 트래커 · 스크랩 (4개). ⚙ 는 상단 유지 (자주 안 들어가서 탭 자리 낭비).
- **`pagedots` = 뉴스 탭 안의 카테고리** — 뉴스 탭에서만 표시(`dots.hidden = pages.length < 2`), 좌우 스와이프도 뉴스 안에서만.
- 같은 탭을 다시 누르면 그 페이지가 최상단으로 스크롤.
- 탭바가 `env(safe-area-inset-bottom)` 을 흡수하므로 `.page-inner` 의 하단 inset 은 제거 (이중 계상이었음).

### 상태 복원 (기사 열고 뒤로 왔을 때)

주인님 설정은 **1번(현재 창에서 열기)** — 기사를 누르면 문서가 통째로 언론사로 이동하므로 앱 UI 가 전부 사라졌다가 뒤로가기로 복귀. 그래서:

- `VIEW_KEY` 스키마를 `{index,scroll}` → **`{tab,index,scroll}`** 으로 확장. 탭을 먼저 복원해야 페이지 목록이 결정되므로 `restoreViewState()` 에서 탭 복원 → `rebuildPages()` → 인덱스/스크롤 순서로 처리.
- `tabIndex` 로 탭별 마지막 카테고리 위치를 따로 기억 (뉴스 탭 3번째에 있다가 다른 탭 갔다 와도 3번째로 복귀).

### 이슈 트래커 (AI 없음)

**AND/OR 키워드 제목 부분일치**만으로 동작 — 외부 AI·요약 API 일절 사용 안 함.

- `collectTrackers()`: 피드 로드/새로고침마다 활성 트래커의 규칙에 맞는 기사를 타임라인에 누적. URL 기준 중복 제거(멱등), 차단된 언론사는 수집 안 함.
- 타임라인 패널: 날짜별 그룹, 그날 보도량 많은 순. **5곳 이상 보도한 날은 마커를 크게** 표시해 "중요한 날"을 보도량으로 자동 판별 (AI 불필요).
- 상태 2개: 진행중(계속 수집) / 종료(수집 중단, 타임라인 보존). 세그먼트로 전환.
- 키워드가 못 잡는 기사는 기사 ⋯ → "이슈에 추가" 로 수동 편입.
- 탭 배지 = **마지막 열람 이후** 새로 모인 건수(`seenAt` 기준). 처음엔 "오늘 날짜" 기준으로 했다가, 피드가 하루 늦으면 배지가 영영 안 뜨는 문제가 있어 변경. 새 이슈 생성 직후의 첫 수집분은 backlog 이므로 seen 처리.

### 스크랩

기사 ⋯ → "스크랩에 저장". 제목·언론사·링크·날짜만 저장 (본문 미저장 — repo 규칙 준수). 저장된 기사는 목록에서 ★ 표시.

### 검증 (headless Edge, 390×844, `_probe_tabs.html`)

**41개 체크 전부 PASS.** 주요 항목:

- 탭바 4개 · 화면 하단 정확히 고정(bottom 705 == innerHeight 705) · 페이저와 겹침 없음(653 == 653) · 가로 오버플로 없음(492/492).
- 대시보드/트래커/스크랩은 1페이지 + 도트 숨김, 뉴스는 18페이지 + 도트 표시, 도트에 "대시보드" 없음.
- 트래커: 실제 피드에서 14건 수집(2일치), **차단 언론사 0건**, 재실행 시 중복 0, 이벤트에 본문 필드 없음.
- 타임라인 패널 z-index 60 으로 탭바를 완전히 덮음(bottom == innerHeight).
- 뷰상태: 탭 저장/복원, 탭별 카테고리 인덱스 기억(index=3 복귀).
- 배지: 미열람 14 → 타임라인 열람 후 사라짐.

⚠️ 첫 실행에서 트래커 4건이 FAIL 했으나 **코드 문제가 아니라 probe 문제**였음 — probe 가 뽑은 키워드가 조선일보 기사에만 있었고, 기본 차단 목록에 있어 정상적으로 걸러진 것. probe 를 "차단되지 않은 기사에서 최빈 단어"를 고르도록 고쳐 해결. 검증 후 `_probe_tabs.html` 삭제 완료.

### 한계 (다음 후보)

트래커는 **수집 시작 이후**의 기사만 쌓임. "이슈 초반 배경 뉴스"까지 채우려면 과거 소급 소스가 필요 — **GDELT 2.0 Doc API** (무료·키 불필요·2017~·날짜 필터 및 `timelinevol` 보도량 그래프 제공) 가 가장 적합. 착수 전 주인님 확인 필요.

## 2026-08-06 — M10 타임라인 카드 · 직접 기록 · GDELT 과거 소급

변경: `site/css/style.css`, `site/js/app.js`.

### 타임라인 표시 개선

첫 구현은 날짜가 그냥 헤더였고 세로줄이 거의 안 보여 타임라인으로 읽히지 않았음(캡처로 확인). 주인님 요청대로 날짜별 카드 형태로 변경:

- 날짜마다 흰 카드, 왼쪽에 레일(●―│―●). 레일을 accent 색 2.5px 로 진하게.
- 날짜 옆 **요일** 표시.
- **5곳 이상 보도한 날은 마커를 크게 + 주황(warn)** — 보도량으로 "중요한 날"을 자동 강조. AI 없음.

### 직접 기록 (수동 보완)

키워드 규칙이 놓치는 것을 손으로 채울 수 있게:

- **＋ 직접 기록 추가** — 날짜 · 내용 · 링크(선택). 링크 없으면 `note:<ts>` 를 url 로 써서 중복 판정 키를 유지.
- 기록 글자를 탭하면 편집, 각 줄 ✕ 로 삭제.
- **삭제한 기사는 `tracker.dropped` 에 URL 을 남겨 재수집을 막는다** — 없으면 새로고침마다 되살아남.
- 수동 편입(⋯ → 이슈에 추가)은 `dropped` 에서 제거해 삭제를 덮어씀.
- 직접 기록은 `newEventCount()` 에서 제외 — 내가 쓴 걸 "새 소식"으로 세면 안 됨.

### GDELT 과거 소급

**실측으로 확정한 사실** (AGENTS.md "확인된 사실"에도 기록):

- **한국어 키워드 검색 불가** (`대통령`/`이스라엘` → 0건). 처음엔 이걸 보고 "GDELT 못 쓴다"고 판단했으나 **오판**이었음.
- **영어 키워드 + `sourcecountry:southkorea sourcelang:korean` → 한국어 기사 정상 수집.** 2025-06-10~24 조회 시 250건 전부 한국어 제목, 보도량이 2025-06-13(선제공습일) 128건으로 정확히 급증.
- **CORS 허용** (`Access-Control-Allow-Origin: *`) → 브라우저 직접 호출 가능.

**아키텍처 판단**: 트래커가 기기별 localStorage 에 있어 **Actions 는 이슈 목록을 볼 수 없다.** 따라서 수집은 브라우저에서. rate limit 은 "사용자가 버튼을 누를 때 한 번만" 으로 회피.

**250건 상한 문제와 해결**: `maxrecords` 최대가 250인데 2주 창도 큰 이슈면 꽉 참 → 오래된 쪽이 통째로 유실. 응답이 250이면 창을 반으로 쪼개 재요청하도록 구현(하루 단위까지). 시뮬레이션 결과 **단순 월 단위 860건 vs 적응 분할 1436건** (전체 1586건 중). 남은 유실은 하루에 250건 넘는 날뿐이고 이건 API 상한이라 불가피 — 사용자에게 "일부만 가져옴"으로 보고.

**UI**: 이슈 편집에 「과거 소급 (영어 키워드)」 + 「소급 시작일」(기본 6개월 전, 사용자 수정 가능). 실행은 타임라인 상단 「↩ 과거 소급」 또는 카드 ⋯ 메뉴. 진행 상황을 구간별로 표시.

### 검증

- **타임라인/직접 기록 probe 19개 PASS** — 카드 렌더, 레일 가시성(opacity 0.3 / 2.5px), 기록 추가·편집·삭제, 삭제 후 재수집 차단, 직접 기록이 배지에 안 세임.
- **GDELT probe 22개 PASS** (fetch 스텁, 합성 코퍼스) — 날짜 헬퍼, en/from 저장, 소급 936건/16요청, 240건 급증일 완전 회수, 2회차 0건(멱등), 한국어 재필터, dropped 유지, 영어 키워드 없을 때 안내.
- **실 API 검증**: 윈도우 분할 6구간 정확, `parseSeenDate` 정상, 라이브 조회 250건/5일치 확인.
- 임시 probe 파일 전부 삭제.

### 이번에 시간을 낭비한 것 (같은 실수 반복 금지)

- **Windows 로컬 프로젝트인데 습관적으로 Bash(빌드서버)로 GDELT 를 호출**했다가 빌드서버 IP 가 상시 429 라 한참 헤맸음. 이 repo 작업은 PowerShell(Windows) 로 할 것.
- 한국어 검색 0건을 보고 성급히 "GDELT 부적합" 결론을 냈으나, 영어 키워드 + 국가 필터로 해결되는 문제였음.

### 소급 결과가 너무 많던 문제 (같은 날 추가 수정)

주인님 지적: "기사를 너무 많이 수집하는 거 아냐? 중요 피더한테만 찾아봐도 될 것 같은데."

**실측으로 확인** (2025-06-10~24 이란 이슈, 250건):

| 도메인 | 건수 |
|---|---|
| fnnews.com | 47 |
| news.mt.co.kr | 28 |
| biz.heraldcorp.com | 26 |
| edaily.co.kr | 20 |
| newspim.com | 20 |

상위 5곳이 141건(56%). 27개 도메인 중 **연합·YTN·SBS·MBC 는 아예 없음** — 양은 많은데 정작 핵심 매체가 빠져 있었다.

**필터 효과 측정:**

| 방식 | 총건수 | 하루최대 |
|---|---|---|
| 원본 | 250 | 139 |
| 화이트리스트만 | 95 | 61 |
| + 제목 중복제거 | 88 | 56 |
| **+ 하루 8건 상한** | **23** | **8** |

⚠️ **제목 중복제거는 거의 효과가 없다**(95→88). 매체마다 제목 표현이 달라 유사도 0.6 에 안 걸린다. **하루 상한이 실질적인 수단.**

**구현**: `MAJOR_DOMAINS` 23곳(통신사·지상파 → 종합일간지 → 경제지 순, 이 순서가 곧 슬롯 우선순위) + `perDay`(3/5/8/15, 기본 8) + 유사도 0.6 중복제거. 소급 패널에서 조절 가능하고 「모든 매체 포함」 체크박스로 필터를 끌 수 있다.

**잡은 버그**: 첫 검증에서 `re-run adds nothing` FAIL(21건 재추가). 상한에 걸려 버린 기사가 `events` 에 없으니 다음 실행 때 다시 받아 넣는 구조였다. `tracker.skippedUrls` 에 기억시켜 해결. 상한을 바꾸면 이 목록을 비워 재검토하게 했다(9건 → 33건으로 늘어나는 것 검증).

**검증 20개 PASS / 0 FAIL** (실제 GDELT 도메인 분포를 그대로 재현한 스텁): 도메인 분류 6종, 기본 실행 248→24건·하루 8건, filtered=153·capped=71 보고, 주요 매체만 남음, 통신사·지상파 우선 배치, perDay=3 반영, allSources 동작, 재실행 0건, 상한 상향 시 증가, 패널 컨트롤 렌더.

### 시작일을 바꿨을 때 (증분 소급)

주인님 질문: "날짜를 변경하면 다시 과거 소급 버튼 눌러서 가져오기하면 되나?"

확인해보니 **동작은 하지만 매번 전 구간을 다시 요청**하고 있었다. 6개월 → 1년으로 늘리면 이미 가져온 6개월까지 또 훑어 시간이 두 배. rate limit 이 빡빡한 API 라 그냥 넘길 문제가 아니었다.

`backfilledFrom` / `backfilledTo` 로 이미 가져온 범위를 기억하고 **새로 열린 구간만** 요청하도록 변경:

- 시작일 앞당김 → 앞쪽 신규 구간 + 마지막 소급 이후 오늘까지만
- 시작일 늦춤 → `upToDate` 반환, 기존 데이터는 그대로 (지우지 않음)
- 변경 없이 재실행 → 요청 0회
- `↻ 처음부터 다시 가져오기` 버튼으로 강제 전체 재수집 가능 (`{force:true}`)

버튼 문구도 상태에 따라 「가져오기 시작」 / 「이어서 가져오기」로 바뀌고, 패널에 `이미 가져온 구간: A ~ B` 를 표시한다.

**검증 18개 PASS / 0 FAIL**: 첫 실행 66건, 재실행 0건·요청 0회, 6월→3월 변경 시 3·4·5월만 요청(6~8월 재요청 없음)하고 92건 추가·중복 0, 시작일 늦추면 upToDate 이면서 범위 안 줄어듦, force 시 6구간 전체 재요청하되 중복 0, 패널 문구·버튼 렌더.

## 2026-08-06 — M11 소스 전환 · 수동/AI 편집

변경: `site/js/app.js`, `site/css/style.css`, `AGENTS.md`.

### GDELT → Google 뉴스로 소급 소스 전환

GDELT 로 만들어놓고 보니 품질이 나빴다. 주인님 지적("중요 피더한테만 찾아봐도 될 것 같은데")으로 실측해보니:

- GDELT 는 파이낸셜뉴스 47 / 머니투데이 28 / 헤럴드경제 26 … **연합·YTN·SBS·MBC 가 아예 없음**
- Google 뉴스 검색 RSS 는 **연합뉴스·연합뉴스TV·BBC** 가 바로 나옴. `after:`/`before:` 로 과거 소급도 됨. 한국어 검색 정상.

⚠️ 그런데 Google 뉴스는 **CORS 헤더가 없어 브라우저에서 직접 못 읽는다.** 무료 중계(allorigins 등)를 거쳐야 하는데 **이게 3회 중 2회 실패**한다(실측). 같은 시각에 /raw 는 죽고 /get 은 살아있는 식.

→ 중계 4곳을 `Promise.any` 로 **동시 시도**(25초 타임아웃), 전부 실패하면 **GDELT 로 자동 대체**. 소스 자체는 서버에서 부르면 항상 200 이므로, 실패하면 중계를 의심할 것.

Actions 로 옮기는 안은 검토 후 주인님이 기각 — 트래커가 기기별 localStorage 라 이슈 목록을 repo 로 옮겨야 하고, 그러면 이슈 추가마다 커밋이 필요해진다.

### 소급 시작일 UI (버그)

주인님: "이란 이슈 실제 시작은 작년이잖아. 날짜를 25년으로 하고 과거 소급해도 그대로야."

원인은 **로직이 아니라 UI** 였다. 시작일이 「이슈 편집」에만 있어서 소급 화면에서는 바꿀 방법이 없었다. 소급 패널에 날짜 입력 + 빠른 선택(6개월/1년/2년/3년)을 직접 넣었다. 검증: 2026-02 → 2025-06 으로 바꾸니 2025-06/07/08/09 구간을 실제로 요청하고 54건 추가.

### 증분 소급

매번 전 구간을 다시 요청하고 있었다. `backfilledFrom`/`backfilledTo` 로 범위를 기억하고 새로 열린 구간만 요청하도록 변경. 실패 구간은 `pendingSpans` 에 남겨 다음 실행 때 이어받는다.

### `Failed to fetch` (실제 버그)

**GDELT 는 429 를 보낼 때 CORS 헤더를 안 붙인다.** 그래서 브라우저가 응답을 통째로 차단하고 `fetch` 가 거부되어, `res.status === 429` 검사에 도달조차 못 했다. → 네트워크 거부를 전부 rate limit 으로 간주, 6/20/45초 점진 백오프, 3연속 실패 시 중단.

### AI 타임라인 붙여넣기

중계가 불안정하니 사람이 AI 를 쓰는 경로를 열었다. 「✦ AI로 채우기」 → 요청문 복사 → AI 답변 붙여넣기 → 파싱 → 미리보기 → 추가.

**프롬프트는 주인님의 실제 실패 세션을 근거로 다시 썼다.** 처음 것은 세 가지를 놓쳤다:

1. 웹 검색 지시가 없어 AI 가 기억으로 답하고 "학습 데이터 범위를 넘어섭니다"
2. "한국 언론 위주로" 라 했더니 yna/ytn URL 이 검색에 안 잡혀 링크가 계속 빔
3. "모르면 비워둬" 를 핑계로 링크를 전부 비움

→ ① 웹 검색 강제 + 핑계 금지 명시, ② 한국 URL 이 안 잡힌다고 알려주고 Reuters·AP·BBC·Al Jazeera 등으로 대체 허용, ③ 모든 줄에 링크 필수·못 찾으면 아예 제외. 예시도 실제 URL 로 교체.

`parseAiTimeline()` 은 관대하게: 파이프·마크다운 표·하이픈/번호 목록·`2025.06.13`·`2025년 6월 14일`·앞뒤 인사말 모두 처리.

### 저장이 안 되던 문제 — 내 실수

주인님: "저장도 안 되는 것 같고."

앱은 정상이었다. **내가 만든 `_demo.html` 이 열릴 때마다 localStorage 를 덮어썼다.** 주인님이 입력한 뒤 내가 "브라우저 띄웠습니다" 하며 데모 링크를 다시 열 때마다 날아갔다. 파일 삭제하고 `index.html` 로 안내. **site/ 에 시드 페이지를 남기지 말 것.**

### 검증 (headless Edge)

- 소스 필터 **20 PASS** — 248건 → 24건, 하루 8건, 통신사 우선 배치
- 증분 소급 **18 PASS** — 재실행 0요청, 날짜 앞당기면 새 구간만, force 전체 재수집
- rate limit 처리 **17 PASS** — CORS 차단·429·본문 안내문 모두 rate 로 처리, 재개 가능
- AI import **34 PASS** — 프롬프트/파서/추가/편집/삭제
- 키워드 없는 이슈 **17 PASS** — 저장·미수집·영속·9건 임포트
- 주인님 실제 대화 데이터 **26 PASS** — 긴 URL·쿼리스트링·`KBS(유튜브)`·제목 속 괄호·같은 날 2건·인사말 제거

도중에 잡은 실제 버그: `2025.06.13` 의 `2025.` 를 목록 번호로 오인해 날짜가 깨지던 것, 링크 있는 항목에 편집 수단이 없던 것, 상한에 걸린 기사가 재실행마다 다시 추가되던 것.

### 병합 (커밋 81d6a5a)

푸시가 거부됨 — 원격에 모르던 커밋 2개(`8dc53ab` 당겨서 새로고침, `297874c` 날씨 위젯 현재 위치)가 있었다. 둘 다 실제 기능이라 rebase 로 내 커밋을 위에 올렸다.

- `style.css`: `.pull-refresh` 스타일(원격) + safe-area 주석(내 것) 양쪽 보존
- `WORK_NOTE.md`: 양쪽 기록 다 남기고 시간순 정렬
- `app.js` 는 자동 병합됐지만 **양쪽이 같은 페이저를 건드리므로** 실제 공존을 검증: 뉴스 18개 페이지 전부 pull-enabled, 트래커·스크랩 탭에는 미적용, 탭바 하단 고정 유지, 가로 오버플로 없음 — **17 PASS**.

## 2026-08-06 — AI 요청문 보강 + 붙여넣기 버튼 (커밋 666fe44)

주인님이 실제 AI 세션 로그를 주셨는데, 요청문 한 번으로는 제대로 안 나오고 4번을 되물어야 했다. 원인 3가지를 프롬프트에 반영:

1. **출력 형식을 보여주기만 했음** → AI 가 표·번호·맺음말을 붙임. `■ 출력 형식 (반드시 지킬 것)` 으로 바꾸고 구분자(`" | "`), 날짜(`YYYY-MM-DD`), 정렬(오래된 것부터), "목록 외에는 아무것도 쓰지 마라" 를 명시.
2. 웹 검색 지시 없음 → "기억에 의존하지 말고 반드시 웹 검색", "학습 데이터 범위를 벗어난다 같은 말 대신 검색해서 확인할 것".
3. "한국 언론 위주로" → yna/ytn URL 이 검색에 안 잡혀 링크가 계속 빔. **한국 URL 이 안 잡힌다고 알려주고** Reuters·AP·BBC·Al Jazeera 등으로 대체 허용, 링크 못 찾은 사건은 아예 제외.

**붙여넣기·지우기 버튼 추가.** 클립보드 읽기는 권한·보안 컨텍스트가 필요해 거부될 수 있으므로, 실패 시 버튼을 "길게 눌러 붙여넣기" 로 바꾸고 입력란에 포커스 (조용히 실패하지 않게).

**잡은 버그**: 「지우기」 후에도 추가 버튼이 이전 건수("＋ 2건 …")를 단 채 활성 상태로 남았다. `redraw()` 의 빈 입력 early-return 이 버튼 상태를 갱신하지 않아서. → 22 PASS.

## 2026-08-06 — 설정 스크롤 · iOS 확대 (커밋 dab867d)

주인님 지적 2건, 둘 다 실제 버그였다.

**① 체크박스 누를 때마다 화면이 위로 올라감.** 설정을 바꾸면 패널 뒤 화면도 갱신해야 해서 `openSettings()` 를 다시 부르는데, `openPanel()` 이 패널을 통째로 새로 만들어 `scrollTop` 이 0 이 됐다. 아래쪽 언론사 차단 목록에서 특히 심함. → `panel.dataset.title` 로 **같은 패널을 다시 그리는 경우에만** 스크롤을 이어받게 수정. 다른 패널은 정상적으로 맨 위 시작.

**② 화면이 가끔 확대됨.** 입력란이 **13px** 이었는데, **iOS Safari 는 16px 미만 필드에 포커스가 가면 페이지를 확대하고 빠져나와도 되돌리지 않는다.** AI 추가 화면에 입력란이 많아 자주 발생. → `.in` 을 16px 로 올리고 `@media (pointer: fine)` 에서만 13px 유지.

⚠️ `maximum-scale=1` 로 막는 방법도 있으나 손가락 확대까지 못 하게 되어 접근성이 나빠진다. 글자 크기로 해결했다.

검증: 터치 에뮬레이션(390×844)에서 스크롤 유지 3종 + 전 화면(AI추가·직접추가·이슈편집·설정) 입력 요소 16px 이상 — **11 PASS**. 데스크톱 실행에서는 의도대로 13px 이 나온다(브라우저가 확대하지 않으므로 정상).

## 2026-08-06 — 타임라인 항목 삭제 확인

주인님: "트래커에서 뉴스 x 누르면 바로 제거되는데 제거할지 묻는 거 추가해. 실수로 지웠어."

✕ 가 기사 링크 바로 옆에 있어 오탭이 쉬운데다, 삭제한 URL 은 `dropped` 에 들어가 **다시 수집되지도 않는다** — 실수하면 되돌릴 방법이 없었다. 타임라인 ✕ 와 항목 편집 화면의 삭제 버튼 양쪽에 확인 단계를 넣었다.

⚠️ **`openSheet()` 를 그대로 쓸 수 없었다.** 이 함수는 `overlayRoot()` 를 비우고 시작하므로, 패널 위에서 부르면 타임라인 패널이 사라지고 취소해도 돌아갈 화면이 없다. 게다가 시트 z-index(50)가 패널(60)보다 낮아 뒤에 가려진다.

→ `confirmSheet()` 를 따로 만들었다. 루트를 비우지 않고 `.sheet-backdrop.stacked`(z-index 80)를 **덧붙이기만** 하고, 취소·배경 탭·확정 모두 자기 노드만 제거한다. 기존 `openSheet()` 동작은 그대로 두었다(기사 ⋯, 트래커 ⋯ 등은 패널 밖에서 열리므로 문제없음).

확인창에는 날짜와 제목(34자 초과 시 말줄임)을 넣어 무엇을 지우는지 보이게 했고, 삭제 항목은 경고색으로 표시했다.

검증 **24 PASS**: 확인창 노출·패널 유지·z-index 80>60·제목 표시, 취소/배경탭 시 항목·패널 보존, 확정 시 삭제·`dropped` 기록·영속·재렌더, 편집 화면 삭제도 동일, 기존 액션 시트 미영향.

## 2026-08-09 — 더보기 · 탭 간 스와이프 · 기사 사진 · 주소 공유

### 사진 — 실측 후 방법을 바꿈

주인님 요청은 "다 넣어달라"였으나 **og:image 추출은 불가능**하다. 실측:

- Google 링크는 리다이렉트되지 않고 JS 중간 페이지를 반환 (12건 시도 → og:image 0건)
- 페이지 HTML 안에 언론사 URL 없음, 기사 id 를 base64 디코드해도 URL 없음(암호화 토큰)
- 이는 `AGENTS.md` 에 이미 있던 제약("HTML 에 원문 URL 없어 서버 측 추출 불가")과 일치

→ **언론사 RSS 를 병행 수집해 제목 매칭으로 사진을 붙이는 방식**으로 전환. `IMAGE_FEEDS` 30개(연합 9 · SBS 2 · 동아 · 매경 3 · BBC 7 · Guardian 5 · ABC · NPR · Variety), 1891건의 이미지 풀을 만들어 제목 완전일치 → 토큰 유사도 0.55 순으로 매칭.

⚠️ **"같은 뉴스면 사진 있는 기사로"** 도 넣었으나 효과는 0이었다. 실측 결과 클러스터 605개 중 **여러 기사가 묶인 것은 6개뿐** — Google 이 이미 사건당 한 건씩만 주기 때문. 코드는 남겨뒀다(수집 소스가 늘면 의미가 생김).

**최종 커버리지 10%** (795건 중 76건). 국내 세계 42% · 스포츠 28% · 경제 24%, 해외 세계 20%. 나머지는 해당 언론사가 RSS 에 사진을 안 실어 불가피하다. 사진 없는 기사는 지금처럼 제목만 나온다.

썸네일은 제목 오른쪽 76×56, `loading=lazy`, 로드 실패 시 요소를 제거해 깨진 아이콘이 남지 않게 했다. http 이미지는 https 페이지에서 차단되므로 수집 단계에서 걸러낸다.

### 더 보기

카테고리당 50건을 한꺼번에 그리던 것을 **15건씩** 나눴다(`PAGE_STEP`). 탭 전환마다 50개 카드를 레이아웃하던 비용이 사라진다. 버튼에 남은 건수를 표시하고, 다 펼치면 숨긴다. 새로 그린 항목도 번역 대상에 포함시킨다(`translatePending(ul)`).

### 탭 간 스와이프

뉴스 탭 안에서는 좌우 스와이프가 카테고리 이동이라 페이저가 제스처를 먹는다. **첫/마지막 페이지에서만** 이웃 탭으로 넘긴다(`setupEdgeSwipe`). 세로 우세 제스처는 당겨서 새로고침에 양보하고, 뒤로 넘어갈 때는 그 탭의 마지막 페이지에 착지한다.

### 주소 공유

헤더에 공유 버튼. `navigator.share` 우선, 없거나 실패하면 클립보드 복사 후 주소를 시트로 보여준다. 사용자가 공유 시트를 취소한 경우(`AbortError`)는 실패로 처리하지 않는다. 공유 주소는 현재 URL 이 아니라 `origin + pathname`(쿼리·해시 제거). 앱 첫 화면에서만 보이도록 대시보드 외 탭에서는 숨긴다.

### 검증

**25 PASS / 0 FAIL** — 공유(주소·제목·탭별 노출), 더보기(15건 상한·남은 건수·증가·소진 시 숨김·전체 도달), 썸네일(https·lazy·빈 alt·76×56·사진 없는 행 정상), 엣지 스와이프 설치, 가로 오버플로 없음, 탭바 고정.

### 대시보드 중요 뉴스도 더 보기 (같은 날 추가)

주인님: "대시보드가 뉴스가 왜 2개밖에 없냐? 근데 왜 2건 5건이야? 날짜로 필터링하나? 더보기 누르면 계속 나와야 하잖아."

**날짜 필터는 없다.** 위젯이 `config.count`(3/5/10)만큼만 그리고 끝내는 구조였다. 게다가 `importantItems()` 가 후보를 상위 **120건으로 잘라** 그 이상은 애초에 닿을 수 없었다.

- 위젯에 「＋ N건 더 보기」 추가. `config.count` 는 이제 **상한이 아니라 처음 보여줄 개수 + 증가 단위**.
- `importantItems()` 의 `pool.slice(0, 120)` 제거 → 정렬된 전체 풀을 훑는다.
- 더 늘어나지 않으면(중복 제거로 소진) 버튼을 감춘다.
- 설정 시트 문구를 "처음 보여줄 기사 수 (더 보기로 계속)" 로 바꾸고 20건 옵션 추가.

검증 **17 PASS**: 5건 시작 → 5건씩 증가 → **707건까지 도달**(755건 중 중복 48건 제외) → 끝에서 버튼 숨김, 중복 렌더 0.

### 사진 비율을 더 올리려는 시도 (10% → 12%)

주인님: "뉴스에서 사진이 제공되는 기사가 저렇게 없어?"

**언론사별로 쪼개보니 원인이 분명했다.** 304개 언론사 중 **284곳이 사진 0건** — 내가 RSS 를 넣은 곳만 붙고 있었다:

```
연합뉴스      24/25  96%   ← RSS 있음
The Guardian   6/8   75%   ← 있음
매일경제       7/14  50%   ← 있음
─────────────────────────
chosun.com     0/28   0%   ← 없음
hankyung.com   0/26   0%   ← 403 차단
hani.co.kr     0/20   0%   ← 사진 미제공
지디넷         0/16   0%   ← 404
```

→ 피드를 30개에서 **51개**로 확대. 새로 찾은 것: 아시아경제(99%) · 뉴시스(100%) · SBS 섹션 4종(100%) · NYT 7종(87~100%) · NBC · Phys.org · Space Daily · The Verge · Ars Technica · Deadline · Independent · BBC 스포츠. 이미지 풀 1891 → **2441건**.

**결과는 10% → 12%.** 풀을 30% 늘렸는데 커버리지는 2%p 만 올랐다.

⚠️ **원인을 확인해보니 매칭 알고리즘 문제가 아니었다.** RSS 를 넣은 곳조차 매칭이 안 되는 경우가 있어 제목을 직접 비교했더니:

- Space Daily: Google 이 준 15건이 RSS 10건과 **아예 다른 기사** (유사도 0.09)
- 아시아경제: 같은 사건인데 제목이 완전히 다름 — Google `망한 단일종목 레버리지ETF 투자…` vs RSS `"물렸다고 그냥 버티면 큰일 난다"…` (0.18)

RSS 는 최신 수십 건만 노출하는데 Google 은 훨씬 넓은 범위에서 뽑는다. **임계값(0.55)을 낮추면 무관한 사진이 붙는다** — 0.4 로 내려도 위 사례는 안 잡히고, 0.2 까지 내리면 엉뚱한 이미지가 달린다. 그래서 유지.

**12% 가 이 방식의 실질적 한계다.** 근본 해결은 Google 피드를 버리고 언론사 RSS 를 주 소스로 쓰는 것인데, 그러면 언론사 다양성이 크게 줄어 별도 판단이 필요하다.

CNN 은 https 로 접속되지 않아 제외(http 이미지는 https 페이지에서 차단됨).

### 언론사 RSS 를 기사 소스로 병합 (사진 12% → 62%)

주인님: "언론사 RSS 로 하면 사진은 잘나오는데 기사가 줄어든다? 해외 뉴스는 아예 안나오나?" → 실측해보니 **정반대**였다.

| | Google 단독 | 언론사 RSS 단독 |
|---|---|---|
| 기사 | 782건 | **2258건** |
| 사진 | 12% | **88%** |
| 언론사 | **304곳** | 23곳 |

기사는 오히려 3배 늘고 해외도 잘 나온다(BBC·NYT·Guardian·NBC·NPR 등 13곳, 806건, 사진 98%). 줄어드는 건 기사가 아니라 **언론사 다양성**뿐. → 주인님 지시로 **둘 다 병합**.

**구현**: `PUBLISHER_SOURCES` 로 피드↔카테고리를 매핑하고, `fetchCategory()` 가 `[...언론사, ...구글]` 순으로 합쳐 기존 클러스터링에 넘긴다. 순서가 중요하다 — 같은 사건이 양쪽에 있으면 **앞에 온 언론사 판이 대표**가 되어 사진과 직접 링크가 살아남는다.

- `parsePublisherRss()`: 언론사 피드는 `<source>` 태그가 없고(피드 자체가 출처) Atom `<entry>`/`<link href>` 를 쓰는 곳도 있어 별도 파서.
- 언론사 기사는 이미 사진을 갖고 오므로, 이미지 인덱스 조회는 **사진 없는 항목에만** 수행.
- `MAX_ITEMS_PER_CATEGORY` 50 → **120** (소스가 둘이고 앱이 15건씩 페이징하므로).

**결과**: 기사 **2016건** · 사진 **62%** · 언론사 **344곳** · 원문 직접 링크 **61%**.

```
경제  91%   주요  92%   세계 86%   사회 84%
해외세계 77%  스포츠 78%   정치 30%   과학  0%
```

- 정치가 낮은 건 Google 검색 RSS 비중이 커서, 과학 0%는 **국내 과학 매체가 RSS 에 사진을 안 실어서**다(동아사이언스·전자신문·뉴시스 전부 확인 — 이미지 태그 0). 해당 카테고리는 구글 단독으로 둔다.
- 카테고리 **안**의 중복은 0건. 전체 216건의 중복은 "주요"와 "세계"에 같은 기사가 있는 것으로 원래 동작.
- CNN 은 https 접속 불가, 한경 403, 한겨레·경향·지디넷은 사진 미제공이라 제외.

검증 **8 PASS**: 2016건·62%·344곳·직접링크 61%, 첫 화면 15건 상한, 썸네일 렌더, 더보기 동작, 가로 오버플로 없음.

### 2026-08-18 — 트래커 타임라인 새 뉴스 확인

- 진행 중인 이슈 타임라인 상단에 `↻ 새 뉴스 확인` 버튼 추가.
- 최신 `news.json`을 다시 읽어 전체 진행 중 이슈를 수집한 뒤, 현재 이슈에 추가된 기사 수 또는 결과 없음/실패를 표시.
- 종료된 이슈에는 버튼을 표시하지 않음.
- `node --check`, `git diff --check`, 로컬 브라우저에서 버튼 표시 및 결과 문구 확인 완료.

### 2026-08-19 — 트래커: 기사에서 바로 추적, 업데이트 일원화, 정렬

**요청**: 뉴스를 보다가 그 기사를 트래커로 등록하고, 업데이트를 누르면 그 뉴스 기준으로 이전·이후 히스토리가 날짜별로 이어지게. 중복 제외, 최초부터 현재까지, 매 업데이트마다 새 뉴스가 이어붙게. 최신순/오래된순 정렬 표시. **AI 는 귀찮아서 안 쓴다.**

**1. 기사 → 이슈 (`trackFromArticle`)**
기사 시트에 `◷ 이 뉴스를 이슈로 추적` 추가. 제목에서 키워드를 뽑아 이슈를 만들므로 검색어를 조립할 필요가 없다. 뽑은 단어는 저장 전에 화면에 보여주고 고칠 수 있게 한다 — 제목의 명사는 어디까지나 추측이라서다.

- `suggestKeywords()`: 대괄호 태그 제거 → 어미(`했다`/`밝혔다` 등 `VERB_TAIL`) 탈락 → 조사 절단(`PARTICLES`) → 불용어(`STOP_WORDS`) 제거 → 앞에서 4개.
- 한국어 형태소 분석은 하지 않는다. 제목 부분일치로 매칭하므로 `이란` 만 남기면 `이란은`도 걸린다.
- 어미를 거르는 이유: `발표했다` 는 무슨 일이 일어났는지를 말할 뿐 **무엇에 관한 이슈인지**를 말하지 않아 키워드로 쓰면 무관한 기사가 딸려온다.
- 씨앗 기사는 키워드 규칙이 자기 제목을 못 맞춰도 타임라인에 무조건 넣는다.

**2. 업데이트 일원화 (`runUpdate` / `openTrackerUpdate`)**
버튼 3개(새 뉴스 확인 / 과거 소급 / AI로 채우기) → **`↻ 업데이트` 하나**. 안에서 최신 피드 수집 + 시작일~오늘 소급을 연달아 돌리고 결과를 한 문장으로 합쳐 보고한다. 최신 뉴스와 과거 뉴스가 서로 다른 경로로 온다는 건 앱 사정이지 사용자가 알 일이 아니다.
`＋직접 추가`·`✦AI로 채우기`·`⚙설정`은 `⋯` 메뉴로 내렸다. AI 는 지웠다기보다 **기본 동선에서 뺀 것** — 쓰던 사람은 ⋯ 에서 그대로 쓸 수 있다.

**3. 중복 제거 — url 이 아니라 제목 기준**
같은 사건이 ①30분 피드 ②Google 소급 ③손으로 추가, 세 경로로 들어오고 **경로마다 url 이 다르다.** url 비교만으로는 통신사 원문과 일간지 재작성본이 둘 다 통과한다. → `같은 날 + 제목 유사도 ≥ 0.6`(기존 `titleTokens`/`similar` 재사용).

- `dedupIndex()` 로 날짜별 버킷을 미리 만든다. 수백 건짜리 타임라인에서 소급을 돌리면 비교가 제곱으로 늘기 때문.
- `collectTrackers()`, `runBackfill()` **양쪽 모두**에 적용. 특히 소급 쪽은 이미 타임라인에 있는 항목으로 인덱스를 채워서 시작한다 — 안 그러면 피드가 이미 모은 기사를 소급이 다른 url 로 다시 넣어 하루가 같은 헤드라인의 재탕으로 찬다.
- 하루 상한(`perDay`)도 **기존 건수를 포함해서** 센다.

**4. 정렬 (`eventsByDay(tracker, order)`)**
`↓ 최신순` ⇄ `↑ 오래된순` 토글. 이슈별로 저장(`tracker.order`)한다 — 진행 중인 사건은 위에서부터 "지금 무슨 일인지"로 읽고, 끝난 사건은 아래서부터 "어떻게 시작됐는지"로 읽기 때문. 정렬을 바꿔도 **하루 안에서는 보도량 순**이 유지되고, 기간 표시(`3-02 ~ 8-01 · 4일`)는 항상 오래된쪽→최신쪽이다.

**검증**
- 단위 30 PASS: 키워드 추출(조사·어미·불용어·한영), 중복 판정(완전·근사·타일·다른날), 정렬(desc/asc/일내 순서/저장/기본값), 요약 문구.
- 헤드리스 브라우저(실 `news.json` 2016건): 최신순 `08-01→06-20→05-11→03-02`, 뒤집으면 `03-02→…→08-01`, 되돌리기·라벨·기간표시·일내 보도량순·재진입 후 유지 전부 확인. 같은 수집을 두 번 돌려 **추가 0건**(중복 차단 확인).
- 프로브는 서버가 `index.html` 에 주입하는 방식으로 돌렸다. `site/` 에 파일을 남기지 않는다.

### 2026-08-19 (2) — 트래커에 뉴스 수동 추가

**요청**: 내가 뉴스를 트래커에 수동으로 추가할 수 있게.

기존에도 손으로 넣는 길은 있었지만 셋 다 부족했다. ①기사 시트의 `기존 이슈에 추가…` 는 눌러도 **아무 반응이 없었고**(시트가 닫히고 뉴스 화면으로 돌아올 뿐), ②이미 들어있는 기사인지 알 수 없었고, ③**트래커 쪽에서 뉴스를 고르는 길이 아예 없었다** — 먼저 피드에서 기사를 찾아내야만 했다.

**1. `pinToTracker()` — 수동 추가를 한 곳으로**
수동으로 꽂은 기사는 **같은 날 비슷한 제목이 이미 있어도 남긴다.** 사용자가 굳이 그 기사를 고른 것이므로 자동 중복 규칙이 그 결정을 뒤집게 두지 않는다. 같은 url 만 거부한다.
`dropped`(삭제 기록)와 `skippedUrls`(상한 초과 기록) 양쪽에서 url 을 빼준다 — 안 그러면 다음 수집이 방금 넣은 것을 도로 걷어낸다.

**2. `toast()` — 결과가 보이지 않는 동작의 확인**
기사를 꽂으면 시트가 닫히고 뉴스 화면으로 돌아오는데, 그 화면에는 추가됐다는 흔적이 없다. `'○○ 에 추가했습니다.'` 를 2.2초 띄운다. 탭바 위에 뜨고 `pointer-events:none` 이라 조작을 막지 않는다.

**3. `addToTrackerSheet()` 개선**
- 이미 들어있는 이슈는 `✓ 이름 (진행중 · 이미 있음)` 으로 표시하고, 누르면 추가 대신 **그 타임라인을 연다**.
- 목록 끝에 `＋ 새 이슈로 추적` 을 붙여 이슈가 없어도 이 시트에서 바로 만들 수 있다.

**4. `openPickArticles()` — 반대 방향 (새로 만든 것)**
이슈를 열어둔 채 **수집된 뉴스 전체를 검색해서 골라 넣는다.** 키워드가 놓친 기사를 넣는 상황에서는 이쪽이 자연스럽다.
- 검색창은 이슈의 첫 키워드로 **미리 채운다** — 빠진 기사는 보통 매칭된 기사들 근처에 있지 전체 피드 맨 위에 있지 않다.
- `enabledCategories()` 가 아니라 **`newsData.categories` 전체**를 뒤진다. 기사가 타임라인에 없는 이유가 바로 평소 필터 밖이라서인데, 숨긴 카테고리도 그 중 하나다.
- 다만 **차단한 언론사는 계속 숨긴다.** 일부러 차단한 매체를 수동 추가 경로로 슬그머니 되살리지 않는다.
- `＋`/`✓` 토글식이라 잘못 넣으면 그 자리에서 뺀다. 40건씩 표시하고 `N건 중 40건 표시` 로 알린다.

**진입점**: 기사 `⋯` 시트 2곳, 타임라인 `⋯` 메뉴, 트래커 카드 `⋯` 메뉴, 그리고 **빈 타임라인 화면**(`＋ 뉴스에서 직접 골라 넣기` — 여기서 ⋯ 를 뒤지게 하면 안 된다).

**검증**
- 브라우저 **21 PASS**: 시트 2경로 노출, 꽂기+manual 플래그, 토스트 표시·문구, 중복 표시·재추가 안 됨, 피커 열림·40건 상한·건수 표시·✓ 표시, 2건 추가/1건 해제·버튼 상태, 해제한 기사가 자동수집으로 되살아나지 않음, 수동 근사중복 유지, 메뉴 2종·빈 화면 진입점.
- 검색 별도 검증: 한글/영문 대소문자 무시, 결과 전건이 검색어 포함, 차단 매체 제외, 없는 단어는 안내 문구.
- 회귀 없음: 기존 단위 30 PASS, 배선 8 PASS.
- `--fg`/`--card` 로 잘못 쓴 CSS 변수를 실제 이름 `--ink`/`--surface` 로 교정(그대로 뒀으면 토스트·피커가 무색으로 렌더).

### 2026-08-19 (3) — 호스팅을 Cloudflare 하나로, 검색 노출 차단

같은 앱이 `news-feeder.pages.dev` 와 `jayhouse2001.github.io/news_feed/` **두 주소로 동시에** 열려 있었다. Cloudflare 로 옮긴 뒤에도 워크플로가 GitHub Pages 에 계속 배포하고 있었기 때문. 방치하면 옛 주소로 들어온 사람이 다른 버전을 보게 되고, 나중에 API 를 붙일 때 origin 이 둘이라 CORS·인증이 복잡해진다.

- 워크플로에서 `configure-pages`/`upload-pages-artifact`/`deploy-pages` 3단계와 `pages: write`·`id-token: write` 권한, `github-pages` environment 를 제거. Cloudflare 배포만 남겼다.
- Cloudflare 단계의 `continue-on-error: true` 도 뺐다. **이제 유일한 호스트**라 배포 실패가 조용히 통과하면 안 된다(GitHub Pages 가 살아있을 때는 보조 수단이라 무시해도 됐다).
- `concurrency.group` 을 `pages` → `deploy` 로. Pages 를 안 쓰니 이름이 맞지 않는다.
- **GitHub 저장소 설정의 Pages 를 Off 로 바꾸는 것은 웹에서 직접 해야 한다** — 워크플로를 지워도 이미 배포된 사이트는 그대로 서비스된다.

**검색 노출**: `site/robots.txt`(`Disallow: /`) + `index.html` 에 `<meta name="robots" content="noindex, nofollow">`. robots.txt 는 크롤러에 대한 *요청*이라 주소를 아는 사람은 그대로 들어온다 — 검색으로 *발견*되는 것만 막는다. meta 태그를 같이 넣은 것은 robots.txt 가 색인을 항상 막지는 못하기 때문(다른 곳에 링크가 걸리면 주소만 색인될 수 있다).

### 2026-08-19 (4) — 계정 로그인 + 서버 트래킹

**요청**: 이메일 매직링크 로그인으로 유저를 구분하고, 트래커 수집을 서버에서.

**왜**: 트래커가 기기 `localStorage` 에만 있어서 ①폰에서 추가한 이슈가 PC 에 안 보이고 ②앱을 열어둔 동안에만 수집되며 ③CORS 릴레이(9~22초, 자주 실패)를 경유해야 했다. 셋 다 클라이언트에서는 못 고친다 — 수집기가 볼 수 없는 이슈를 수집할 수는 없으니까.

**구조**
```
GitHub        소스만
Cloudflare    Pages(앱) + Functions(API) + D1(DB) + Worker(30분 수집)
Resend        로그인 메일
```
Pages Functions 는 cron 을 못 걸어서 수집 Worker 를 분리했다. 같은 D1 을 바인딩한다.

**로그인**: 비밀번호 없음. 토큰은 15분·1회용이고 확인 즉시 삭제해 재사용을 막는다. 세션은 6개월 HttpOnly 쿠키 — 매일 보는 앱에 몇 주마다 메일을 보내는 건 말이 안 된다. 첫 로그인이 곧 가입. **링크 요청 응답은 계정 존재 여부를 알려주지 않는다**(알려주면 이 엔드포인트가 "누가 여기 가입했나" 조회기가 된다).

**격리**: 모든 트래커 쿼리가 SQL 단계에서 `user_id` 로 거른다. 가져온 뒤 비교하는 게 아니라서 uuid 를 알아도 남의 이슈를 못 읽는다. 테스트로 확인.

**앱 동작 (계정 없이도 그대로)**
- 로그아웃 상태: 지금까지처럼 `S.trackers`(localStorage). API 가 없어도 앱은 정상.
- 로그인 상태: `remoteTrackers` 를 읽는다. 서버 사본을 localStorage 에 쓰지 않으므로 로그아웃해도 기기 데이터가 오염되지 않는다.
- `trackerList()` 한 곳에서 어느 목록을 쓸지 정한다.
- **설정(차단 언론사·위젯·날씨)은 올리지 않는다.** 기기마다 다른 게 자연스럽고 동기화하면 오히려 불편하다.
- 목록 API 는 이벤트 대신 건수와 **최신 헤드라인 1건**만 준다. 대시보드 위젯이 그걸 쓰는데 없으면 전부 "모인 기사 없음"이 된다. 타임라인은 열 때 가져온다(`_partial`).

**이전**: 기기 이슈를 계정으로 **복사**한다. 원본을 지우지 않아서 잘못돼도 남아 있고, 그 전에 백업도 받게 한다. 기사는 한 건씩 올린다 — 중간에 실패해도 기사 하나만 잃지 타임라인 전체를 잃지 않는다.

**수집 튜닝 (실측으로 발견)**: 인기 키워드는 어떤 창을 요청해도 100건이 꽉 차서, "응답이 가득 차면 쪼갠다" 규칙만으로는 하루 단위까지 재귀했다. 실측 20일 구간에 **24요청 / 수집 60 / 버림 419 / 미완료 / 16초**. 4일 미만은 쪼개도 하루 상한이 대부분 버리므로 분할을 멈추게 했더니 **15요청 / 수집 94 / 완료 / 3.4초**. 요청은 줄고 수집은 늘었다.

**검증**
- 서버 단위 **54 PASS**: 로그인·토큰 재사용/만료·계정 격리(uuid 알아도 못 읽음)·CRUD·cascade·이벤트 추가/삭제/재추가.
- 수집 **23 PASS**: 실제 Google 뉴스 + 스텁(중복 병합·통신사 우선·하루 상한·제외 목록·AND 규칙·allSources·실패·검색어 없음).
- 클라이언트 통합 **40 PASS**: 로그아웃→로그인→이전→서버 생성→지연 로딩→쓰기 반영→서버 수집(74건/11요청)→로그아웃 복귀.
- **API 없는 환경 20 PASS**: 계정 확인 실패해도 앱 정상, 로컬 트래커 전 기능 동작, 로그인 화면은 멈추지 않고 실패를 표시.
- D1 대역은 `node:sqlite`(Node 24 내장) 인메모리로 세웠다. 실제 핸들러를 그대로 돌린다.

**남은 것**: 뉴스 수집을 GitHub Actions → Cloudflare 로 옮기기(수집기 실측 4초, Workers cron 30초 제한 안에 충분). Cloudflare 설정은 `SERVER.md`.

### 2026-08-19 (5) — 트래커 등록 시 키워드 선택

**요청**: 등록할 때 키워드 추가/삭제가 처음부터 되게. (제목에서 중요하지 않은 단어가 키워드로 들어감)

추가/삭제 자체는 이미 있었다. 진짜 문제는 **뽑히는 단어가 나빠서 매번 지워야 했던 것**이다.

**측정한 실패**
```
트럼프 "호르무즈 완전 장악"...이란, '휴전 연장 합의' 부인
  → 트럼프, 호르무즈, 완전, 장악      ← 完全·掌握은 아무것도 못 거름

중대본 "고령 농업인·야외 근로자 안전"…폭염대응 점검
  → 중대본, 고령, 농업인, 야외        ← 핵심인 '폭염'이 빠짐 (앞에서 4개만 잘라서)

이강인, 오늘 밤 서울에서 아틀레티코 입단식
  → 이강인, 서울, ...                ← '서울'은 전체 2016건 중 26건에 등장
```
원인 셋: ①빈도를 안 봄 ②**위치로 잘라서** 뒷부분 핵심어 유실 ③서술어 무방비.

**시도했다 버린 것 — 순수 희귀도 정렬**
문서빈도가 낮은 순으로 고르게 했더니 평균 빈도가 4.3 → 2.0 으로 "개선"됐는데 결과는 더 나빴다:
```
  개선(?) → 완전(0), 장악(1), 휴전(0), 부인(0)   ← 트럼프·이란·호르무즈가 전부 탈락
```
**희귀하지만 무의미한 단어**가 흔하지만 핵심인 고유명사를 이긴다. 숫자가 좋아진 것이 품질이 좋아진 게 아니었다.

**채택한 것 — 점수제 (`rankKeywords`)**
- `WEAK_WORDS`: 서술어·정도어(완전·부인·장악·주장…) −100. 어미 규칙·불용어·희귀도 어디에도 안 걸리면서 노이즈만 만드는 부류.
- 문서빈도: 2~8건 +3 / 1건 이하 +1 / 15건 이하 +1 / 그 이상 −3
- 영문·숫자 포함 +2 (한국어 제목에서 라틴 문자는 거의 항상 고유명사 — LAFC, G7)
- 3글자 이상 +1, 제목 앞쪽(0~1번째) +1

**⚠ 비율이 아니라 절대 건수로 잡아야 한다.** 처음에 `total * 0.02` 로 했더니 `서울`(26/2016 = **1.3%**)이 만점을 받았다. 어떤 백분율 기준을 써도 통과한다. 말뭉치 크기와 무관하게 `호르무즈`(3건)와 `서울`(26건)을 가르는 건 절대 건수다.

**UI**: 제목의 **모든 단어를 칩으로** 보여주고 점수 상위 **2개만 켜둔다**. 어떤 단어가 이 사건을 지칭하는지는 사람이 즉시 판단하는 것이고 점수는 추측일 뿐이라, 순위는 *기본값*만 정하고 *선택지*는 제한하지 않는다. 누르면 켜고 꺼진다. 제목에 없는 단어는 입력으로 추가.

기본값을 4개 → **2개**로 줄였다. 좋은 키워드 2개가 어중간한 4개보다 타임라인이 깔끔하다.

**검증**
- 키워드 **21 PASS**: 고유명사 후보 포함, 서술어 음수 점수, 기본값에 약한 단어 없음, `서울` 제외되지만 칩으로는 제공, 라틴 이름 우선, 칩 토글·aria-pressed, 직접 입력 추가/삭제, 키워드 0개면 저장 거부.
- 회귀 **12 PASS**: 등록 → 저장 → 수집 → 타임라인 → 이슈 편집기(기존 AND/OR 칩 UI 그대로).
- 기존 단위 **30 PASS** 유지.
- 후보가 하나도 안 나오는 제목: 2016건 중 **5건(0.2%)**.

### 2026-08-19 (6) — 뉴스 수집을 Cloudflare 로 이전

**요청**: 소스 제외하고 다 Cloudflare 로.

**왜 옮기나**: 수집기가 GitHub Actions 와 Cloudflare cron 두 곳으로 갈라질 상황이었다(트래커 수집은 Cloudflare 로 만들어 둔 상태). 같은 주기에 같은 종류의 일을 하는 것이 두 군데 있을 이유가 없다. GitHub 무료 cron 은 **정시에 안 돈다**(5~15분 지연, 심하면 건너뜀)는 것도 있다.

**구조 변경**
- `shared/collect-news.js` — 수집 로직을 런타임 중립 모듈로 분리. `fetch` 와 문자열 처리만 쓴다.
- `scripts/fetch-news.mjs` — 이제 그 모듈을 불러 파일로 쓰는 **얇은 래퍼**(20줄). Node 전용 코드는 원래 파일 쓰기 4줄뿐이었다.
- `worker/cron.js` — 30분마다 **뉴스 + 트래커 둘 다**. `Promise.allSettled` 로 묶어 한쪽 실패가 다른 쪽을 죽이지 않게 했다(뉴스는 모든 독자가 의존하므로 트래커 오류에 끌려 내려가면 안 된다).
- `functions/api/news.js` — KV 에서 읽어 서빙. 5분 캐시.
- `site/data/news.json` — **여전히 배포에 포함**한다. 새 배포 직후 cron 이 아직 안 돌았거나 KV 가 안 붙은 상태에서 앱이 비어 보이면 안 된다. `loadNews()` 가 `api/news` → 실패하면 파일 순으로 시도한다.

**KV 를 쓴 이유**: 뉴스는 950KB 짜리 덩어리를 페이지 로드마다 통째로 읽는다. D1 은 조건으로 골라 읽는 DB 라 이 용도엔 무료 읽기 한도만 더 태운다.

**Actions 는 배포 전용으로 남긴다**: cron 제거, 푸시 때만 실행. fallback `news.json` 을 만들고 Cloudflare 로 업로드한다. 수집 실패는 `continue-on-error` — 오래된 fallback 이 없는 fallback 보다 낫고, 실제 피드는 Worker 가 공급한다.

**⚠ HTTP 헤더에 한글을 넣으면 던진다.** `X-News-Source: 'KV 비어있음'` 으로 썼다가 `Response` 생성 시점에 `TypeError: Cannot convert argument to a ByteString` 이 났다. 헤더 값은 Latin-1 이다. `kv-empty` 같은 ascii 로 바꿨다. **테스트로 잡았지만 배포하면 그대로 터졌을 자리.**

**검증**
- 분리 전후 산출물 동일: 카테고리 18, 필드 구조 동일, 기사 2016 → 2033(그 사이 갱신분).
- Worker **28 PASS**: 비밀키 가드(4가지), 실제 피드 수집 → KV 저장(2031건·953KB·**2.5초**, cron 제한 30초), 18개 카테고리·사진 1378건 보존, 메타데이터, `/api/news` 서빙, KV 미연결/빈 KV/오류 3가지 fallback, 트래커 수집 동시 실행, 만료 토큰·세션 정리, **한쪽 실패 격리**.
- 앱 **7 PASS**: `/api/news` 없는 환경에서 파일 fallback 으로 정상 동작.

**미결**: Cron Worker 미배포. KV 생성 + Pages 에 `NEWS` 바인딩이 필요하다(`SERVER.md` 4절). 배포해도 **트래커 수집은 여전히 0건** — Google 이 Cloudflare IP 를 막는다. 뉴스 수집은 언론사 RSS 라 영향 없다.

### 2026-08-19 (7) — Cron Worker 배포 + KV 연결

**배포를 Actions 로 옮겼다.** 처음엔 주인님이 터미널에서 `wrangler deploy` 를 쳐야 하는 구조였는데, Worker 는 스케줄과 수집 코드를 함께 갖고 있고 앱은 그 결과를 읽는다. 따로 배포하면 푸시 한 번에 한쪽만 움직일 수 있고, 증상은 "피드가 조용히 멈춘다"뿐이라 알아채기 어렵다. → 같은 워크플로에서 Pages 와 Worker 를 함께 배포한다.

기존 `CLOUDFLARE_API_TOKEN` 에 Workers 권한이 있어서 그대로 됐다(확인: `/run` 이 403 응답 = 가드 작동 = 배포됨). 권한이 없는 경우를 대비해 `continue-on-error` 를 걸었다 — Pages 배포는 어차피 성공하므로 Worker 실패가 전체를 막을 이유가 없다.

**바인딩 상태 추적**: `X-News-Source` 헤더로 어느 경로인지 알 수 있게 해둔 것이 설정 과정에서 그대로 진단 도구가 됐다.
```
file (no-binding) → KV 바인딩 없음
file (kv-empty)   → 바인딩 됐고 cron 이 아직 안 돎
kv                → 정상
```
대시보드에서 바인딩을 추가해도 **이미 존재하는 배포에는 적용되지 않는다** — 빈 커밋을 푸시해 재배포해야 반영된다. D1 때도 같은 함정이 있었다.

**KV 네임스페이스**: `3ac943558c2a438b8834e949a26ebc07`. Pages 와 Worker 양쪽이 `NEWS` 라는 같은 이름으로 바인딩한다(Worker 가 쓰고 `/api/news` 가 읽는다).

### 2026-08-19 (8) — cron 이 "all categories failed" 로 실패한 원인

**진단이 먼저 필요했다.** 40분간 KV 가 안 채워지는데, 원인이 "cron trigger 미등록"인지 "실행됐지만 던졌다"인지 구분할 방법이 없었다 — 둘 다 *아무것도 안 쓰이고 앱은 fallback 을 계속 서빙*하는 똑같은 증상이다. → 매 실행을 성공·실패 무관하게 KV(`cron:last`)에 기록하고 `/api/news` 가 `X-Cron-Last` 헤더로 노출하게 했다. 다음 확인에서 바로 답이 나왔다:

```
{"at":"08:00:45","trigger":"cron","news":{"error":"all categories failed"}}
```
**trigger 는 정상이었다.** 수집이 실패한 것.

**진짜 원인: Workers subrequest 제한 (한 실행에 외부 요청 50개).**
```
n=6/12/30 → 전부 성공
n=51      → "Too many subrequests by single Worker invocation"
```
수집기는 116 요청을 했다(이미지 51 + 언론사 47 + Google 18). 이 PC 에는 제한이 없어 잘 됐던 것.

**중복 발견**: 이미지 피드 51개 중 **47개가 언론사 피드와 같은 URL**이었다. 같은 피드를 기사용·이미지용으로 **두 번** 부르고 있었다. 한 번 받아 양쪽에 쓰게 하니 116 → 69.

**그래도 69 > 50** 이라 카테고리를 2개 슬라이스로 나눴다:
- 슬라이스 0: 카테고리 9 + 언론사 24 + Google 9 + 이미지전용 4 = **37 요청**
- 슬라이스 1: 카테고리 9 + 언론사 23 + Google 9 = **32 요청**

**cron 은 시계로 슬라이스를 고른다** (`floor(now/30분) % 2`). 어디까지 했는지 기억할 필요가 없다. 슬라이스는 KV 에 쌓아두고 **전부 모이면 한 번에 발행**한다 — 슬라이스마다 발행하면 앱이 반쪽 피드를 보게 된다.

**사진 비율 유지**: 슬라이스별로 자기 피드만 색인하면 67.6% → 65.9% 로 떨어진다. 모든 슬라이스의 피드 본문을 모아 색인을 한 번 만들고 재매칭하니 **66.9%** 로 회복(측정치 변동 범위 내).

**테스트가 잡은 버그**: `run(env, trigger)` 가 `slice` 를 받지 않아 전체 수동 실행(`/run?slice=N`)이 슬라이스를 지정할 수 없었다. `only=news` 경로만 동작했다.

**검증 180 PASS**: 슬라이스 19(요청 수 37/32 실측·부분 발행 안 함·병합 18카테고리·중복 없음·사진 유지·시계 기반 순환·재실행 안전) + API 54 + 수집 23 + DB 9 + 진단 17 + cron 28 + 클라이언트 30.

### 2026-08-19 (9) — 트래커 자동 수집을 언론사 기사 매칭으로

**문제**: 서버에서 Google 뉴스를 못 쓴다(Cloudflare egress 에 503). 직접·릴레이 5개 전부 차단이라 우회로가 없었다.

**해법**: 검색이 필요한 것과 아닌 것을 나눴다.
- **과거 히스토리** = 검색이 필요하다 → **브라우저**에서 (주인님 IP 는 통과)
- **오늘 새 기사** = 검색이 불필요하다 → **서버**에서 매칭

cron 은 이미 뉴스 피드 전체(2036건)를 받는다. 추적 중인 이슈의 새 기사는 **그 안에 있다.** 받아둔 기사에 키워드를 대보면 되므로 **추가 요청 0**. Worker 요청 한도가 50인 상황에서 이게 결정적이다.

`functions/_lib/match.js` — `matchArticles(env, articles)`. 뉴스 발행 시점에 호출된다(기사가 이미 메모리에 있는 지점).

**중복 규칙은 브라우저와 동일**: url + `같은 날 + 제목 유사도 0.6`. 통신사 재작성본을 걸러야 하는 건 서버도 마찬가지다. `excluded_urls`(사용자가 지운 것)도 존중한다.

**`sweepTrackers` → `maintain`**: 수집이 뉴스 런으로 옮겨갔으니 이 스케줄에 남은 건 만료 정리뿐이다. Worker 번들 31.9 → 25.4 KiB (Google 소급 코드가 빠져서).

**실측 확인**: 실제 뉴스 런에 `대통령` 이슈를 걸어두니 8건이 들어왔다. 전부 제목에 키워드 포함, 날짜 정상.
```
2026-08-19 | 연합뉴스 | 여야 용산공원 공방…"盧 뜻이자 李대통령 공약"
2026-08-19 | 매일경제 | 민주당은 文·국힘은 朴…같은날 '전직 대통령' 찾은 여야
```

**검증 203 PASS**: 매칭 21(요청 0 확인·재실행 무변화·재작성본 병합·다른 날 유지·하루 상한·제외 목록·종료/무규칙 스킵·AND·다중 이슈·잘못된 기사 방어) + 슬라이스 22(실제 런이 트래커를 채우는 통합 테스트 포함) + API 54 + 수집 23 + DB 9 + 진단 17 + cron 27 + 클라이언트 30.

**슬라이스 방식 실측 확인** (08:30 cron): `{"slice":1,"sliceOnly":true,"have":1,"of":2,"ms":3078}` — 이전 `all categories failed` 에서 정상 수집으로 바뀌었다. 09:00 에 나머지 슬라이스가 들어오면 발행된다.

### 2026-08-19 (10) — 흐름이 읽히는 타임라인

**요청**: 뉴스 양이 아니라 이슈의 흐름 파악. 하루에 한두 건만 보이고, 각 기사 옆 더보기로 그날 비슷한 뉴스를 더 볼 수 있게.

**하루 상한 8 → 2** (선택지 `3/5/8/15` → `1/2/3/5`). 실측: 사건이 터진 날은 26건이 쏟아지는데 그중 하나만 골라도 흐름은 그대로 읽힌다. 오히려 8건씩 쌓이면 반복이 사건의 모양을 덮는다.

`migrations/0002_per_day_default.sql` — **기존 이슈 중 옛 기본값(8)인 것만** 2로 바꾼다. 사용자가 직접 고른 5나 15는 건드리지 않는다. SQLite 는 컬럼 기본값을 못 바꾸고, 테이블을 재작성하는 건 아직 생기지도 않은 행의 기본값을 위해 실제 타임라인을 위험에 빠뜨리는 짓이다 — 새로 만드는 것은 `validateTracker()` 가 처리한다.

**더보기 — 수집은 다 하고 화면에서만 접는다.** 상한을 수집 단계가 아니라 표시 단계에 두면 ①더보기가 공짜고 ②숫자를 바꿔도 재수집이 필요 없다. `DAY_PREVIEW = 2`, 넘치는 만큼 `⌄ 이 날 기사 N건 더 보기` 버튼. 카드 안에 넣어 그날의 일부로 읽히게 했다.
- 날짜 헤더는 **접힌 상태에서도 진짜 총계**(`7건`)를 보여준다.
- 미리보기는 **보도량 순** 상위 2건 — 그날 가장 많이 다뤄진 것이 먼저.
- 펼침 상태는 **정렬 전환·삭제·새로고침을 넘어 유지**된다(타임라인은 매번 새로 그려지므로 방금 펼친 것이 닫히면 그것도 버그다). 단 **이슈 id 에 묶어서**, 다른 이슈를 열면 물려받지 않는다.

**스팸 차단 (`BLOCKED_SOURCES`)**: 실측에서 나온 `Gwara Med`(로스트아크·도박 글이 검색어를 본문에 심어 통과), `Vietnam.*`(같은 문장 자동번역 반복). **"모든 매체 포함"을 켜도 적용된다** — 그 스위치는 주요 매체 목록을 넓히는 것이고 바닥을 없애는 게 아니다. 하루 2건일 때 한 칸을 스팸이 차지하면 절반을 잃으므로 상한을 줄인 것과 짝이다.
`tracker.js` 에 두고 `sweep.js`·`match.js`·`app.js` 세 곳이 공유한다(각자 복사본을 두면 한 곳만 고치는 일이 생긴다).

**검증 229 PASS**: 표시 16(미리보기 2건·헤더 총계 유지·보도량 순·펼침/접기·정렬 전환 후 유지·다른 이슈 격리) + 차단·기본값 26(실측 스팸 5종 차단·정상 매체 8종 통과·allSources 무력화 안 됨·마이그레이션이 사용자 설정 보존) + 기존 187.

### 2026-08-20 — 과학 카테고리 빈 화면, 트래커 타임라인 구멍

주인님 신고 두 건. 둘 다 **Google 이 Cloudflare 를 막는다**는 같은 뿌리에서 나왔는데, 이전 작업에서 놓친 곳들이다.

**1. 과학 0건**
18개 카테고리 중 **과학만 언론사 피드가 하나도 없었다.** Google 단독 의존 → 서버에서 503 → 통째로 빈 화면. 다른 카테고리는 언론사 피드가 받쳐줘서 살아남았다. (예전에 "국내 과학 매체가 RSS 에 사진을 안 실어서" Google 단독으로 뒀던 결정이 발목을 잡았다 — 사진 없는 카테고리가 기사 없는 카테고리보다 낫다.)

붙인 피드: 헬로디디, 로봇신문. **전자신문은 붙였다 뺐다** — RSS 섹션이 종합뉴스뿐이라 싱크홀·성폭행 기사가 과학에 들어왔다(Section901~904 전부 확인).

검증: **Google 전면 차단 상태로 수집** → 18개 전부 채워짐, 과학 117건. 슬라이스 40/32 요청으로 한도 내 유지.

**2. 트래커가 2026-02 에서 2026-08 로 건너뜀**
소급 시작일 2024-08-19 → 오늘 = **월 단위 창 25개**, 그런데 `MAX_REQUESTS = 24`. 게다가 전쟁이 터진 시기는 응답이 가득 차 반으로 쪼개져 큐에 다시 들어간다. 큐가 **오래된 쪽부터** 소진되니 2024년 조용한 구간을 훑다 예산을 다 쓰고, **정작 전쟁이 난 2026-03~08 은 요청조차 안 됐다.** 재현 테스트로 확인: 오래된 것부터면 마지막 도달 창이 정확히 `2026-02-19` — 주인님이 본 그 지점.

→ `queue.reverse()`. 최신 창부터 채운다. 예산이 모자라도 독자가 보려는 쪽이 먼저 찬다.

**3. 로그인 상태면 소급이 아예 안 됐다**
`runUpdate` 가 서버 API 를 호출했는데, 서버는 Google 을 못 쓴다. **로그인한 사용자는 과거 소급이 통째로 실패**하고 있었다(오류도 아니고 0건으로 조용히). → 브라우저가 검색하고 결과를 서버로 올린다(`uploadEvents`). `/update` 엔드포인트는 `clientSweep: true` 를 돌려주고 스스로 수집하지 않는다.

**4. `미국` 같은 넓은 키워드**
실측: OR `[전쟁,미국,이란,이스라엘]` → 29건 매칭 중 **11건 무관(정확도 62%)**. 단어별로 보면 `미국` 12건 중 관련 1건 — 노이즈의 전부가 이 단어였다. 국가부채·알트코인·ICC 제재가 "미국-이란 전쟁" 타임라인에 들어온 이유.

정적 경고문("흔한 단어는 피하세요")은 행동으로 옮길 수 없는 조언이라, **숫자를 보여준다**: 칩에 문서빈도 배지(10건 이상이면 점선 테두리 + 숫자), 선택 즉시 `지금 피드에서 N건 걸립니다` 와 실제 제목 3개 미리보기. 실측 확인 — 넓은 단어를 켜면 6건 → 17건으로 뛰는 게 바로 보인다.

**같은 이름 다른 시그니처 함정**: `trackerMatches(tracker, item)` 는 앱, `trackerMatches(tracker, title)` 는 서버. 문자열로 부르니 `toLowerCase` 에서 던졌다. 문서화 대신 **양쪽을 다 받게** 고쳤다.

**검증 239 PASS** (키워드 미리보기 10 · 순서 6 · Google 차단 4 + 기존 219).

**5. 기존 날짜 동결** (주인님 요청 중 남아있던 것)

"새로고침하면 이미 있는 날짜는 더 이상 업데이트하지 말고 최신 뉴스만 추가" — 확인해보니 **그렇게 동작하지 않고 있었다.**
- 서버(`matchArticles`)는 하루 상한만 볼 뿐, 그 날이 이미 자리 잡았는지는 안 봤다.
- 앱(`collectTrackers`)은 **하루 상한 자체가 없었다** — 오래된 날짜가 계속 자랄 수 있었다.

양쪽에 `settled` 를 넣었다. 수집 시작 시점에 타임라인이 이미 가진 날짜 집합을 만들고, 그 날짜의 기사는 건너뛴다. 지난주 기사가 새로 나와도 지난주에 일어난 일이 바뀌지는 않고, 읽던 날이 조용히 다시 쓰이는 게 빠지는 것보다 나쁘다.

**영향 없는 것들** (의도적으로 구분):
- **과거 소급**(`runBackfill`/`sweepTracker`) — 오래된 날짜를 채우는 게 목적이므로 동결하지 않는다.
- **수동 추가** — 다른 경로라 자리 잡은 날에도 그대로 들어간다. 자동 수집만 막는 것이지 사용자를 막는 게 아니다.

앱 쪽에는 빠져 있던 하루 상한과 스팸 차단도 같이 넣었다(서버에만 있었다).

검증 **7 PASS**: 자리 잡은 날 불변, 새 날짜는 수집됨, 새 날짜에 상한 적용, 빈 트래커는 전부 수집 가능, 수동 추가 영향 없음.

### 2026-08-20 (2) — 개인 AI 키로 트래커 보강 (Claude·ChatGPT·Gemini·Grok)

**요청**: 키워드 방식으로는 안 되겠다, 개인 AI 토큰을 넣어서 AI로.

**왜 필요했나**: 키워드는 *"이 단어를 포함한다"* 와 *"이 이슈에 관한 것이다"* 를 구분하지 못한다. `미국` 이 국가부채·알트코인·ICC 제재를 전쟁 타임라인에 넣은 이유이고, 규칙을 아무리 다듬어도 이 구분은 안 된다.

**구조**: `functions/_lib/ai.js` 가 네 제공자를 한 모양으로 감싼다. 셋은 OpenAI 채팅 형식이고 Claude 와 Gemini 는 각자 형식이라 요청 빌더·응답 리더를 제공자별로 뒀다. 새 제공자를 넣으려면 이 파일 하나만 고치면 된다.

**왜 서버를 거치나**: 브라우저에서 네 곳 모두 CORS 로 막는다. `/api/ai` 는 그래서 존재하고, **키를 받아 한 번 쓰고 버린다** — 저장·로깅·D1 기록 전부 없다. 계정도 필요 없어서(`plainHandler`) 로그인 여부와 무관하게 동작한다. 키는 `localStorage` 에만 있다.

**세 가지 작업**
- **filter** — 수집된 기사 중 이슈와 무관한 것을 골라낸다. *무엇을 지울지 먼저 보여주고 확인을 받는다.* 삭제는 `removeEvent` 를 거쳐 기록되므로 다음 수집이 되살리지 않는다. 100건씩 나눠 부른다.
- **summary** — 타임라인을 읽고 사건 전개를 정리한다. 저장하지 않는다(다시 만들면 되고, 저장하면 낡는다).
- **keywords** — 이슈 이름만 주면 규칙을 제안한다. 연결 확인에도 이 작업을 쓴다(가장 쌈).

**JSON 추출이 실제 난제였다.** 형식을 아무리 못 박아도 모델은 앞뒤에 말을 붙인다. 코드펜스, 앞말, 뒷말, 문자열 안의 중괄호, 이스케이프된 따옴표까지 견디는 추출기를 따로 뒀다 — 10가지 형태로 검증.

**방어**: 한 번에 기사 120건·이벤트 200건 상한(폭주가 사용자 할당량을 한 번에 태우지 않게), 모델이 범위 밖 인덱스를 주면 버림, 401/403→"키 확인", 429→"한도", 타임아웃 구분.

**비용**: Haiku 기준 기사 100건 필터링에 약 $0.01. 이슈 5개를 매일 돌려도 월 몇 백 원.

**검증 305 PASS**: AI 63(네 제공자 요청 형태·키 전달 방식·응답 읽기·오류 분류·JSON 추출 10종·검증·키 미노출) + 앱 32(제공자 전환·키 저장·연결 확인·필터 전 과정·확인 후 삭제·요약 렌더·키 없을 때 안내·메뉴 노출) + 기존 210.

**line ending 사고**: 패치 스크립트가 파일 전체를 다시 써서 `\r\r\n` 이 생겼고 git 이 전 줄을 변경으로 봤다. 정규화 후 366줄 추가로 정상화. **전체를 다시 쓰는 패치는 줄바꿈을 한 번만 정규화할 것.**

### 2026-08-20 (3) — AI 웹 검색으로 타임라인 생성

**요청**: 이슈 추가에서 "언제부터"와 "무슨 내용"만 쓰면 나머지는 AI 가. 날짜를 비우면 최초 시작부터 알아서 찾아서. 그리고 결과가 기존 타임라인처럼 연결된 리스트로 보이는지 확인.

**⚠ 확인 없이 단정한 것 두 번**: "OpenAI·Grok 은 웹 검색이 안 된다"고 했는데 **넷 다 된다.** 그리고 그 전에 모델명 세 개를 기억으로 써서 전부 틀렸다. 주인님 지적이 맞고, 이번엔 네 제공자 문서를 전부 읽고 적었다.

**웹 검색 (2026-08-20 문서 확인)**
| | 켜는 법 | 엔드포인트 | 출처 URL |
|---|---|---|---|
| Claude | `tools:[{type:"web_search_20250305"}]` | 그대로 | ✅ citations |
| Gemini | `tools:[{google_search:{}}]` | 그대로 | ✅ url_citation |
| Grok | `search_parameters:{mode:"on"}` | 그대로 | ✅ citations |
| ChatGPT | `tools:[{type:"web_search"}]` | **`/v1/responses`** | ✅ url_citation |

ChatGPT 만 엔드포인트가 다르다 — chat/completions 에는 검색 전용 모델로만 있다. 요청·응답 형태가 달라서 `read()` 가 두 형태를 모두 처리한다.

**비용 (문서 확인)**
| | |
|---|---|
| **Gemini** | **월 5,000회 무료**, 이후 $14/1,000 |
| Grok | $5/1,000 |
| Claude · ChatGPT | $10/1,000 |

Gemini 가 압도적이라 기본으로 둘 만하다. 단 무료 티어에서는 검색이 안 되고, 유료 티어(카드 등록)에서 월 5,000회가 무료다. 이슈 하나에 검색 14회 상한이니 월 350개까지 공짜.

**`timeline` 작업**: 이슈 이름(+선택 시작일)만 받아 웹 검색으로 날짜별 기사를 찾는다. **시작일을 비우면 "이 사건이 언제 시작됐는지 직접 판단해서"** 라고 프롬프트에 넣는다 — 독자도 모르는 게 보통이라 이게 기본이다.

**지어낸 항목 차단이 핵심이다.** 모델은 형식만 채우려 들고, 가짜 링크는 열리지 않는 타임라인 항목이 된다. 파서가 ①`YYYY-MM-DD` 아닌 날짜 ②빈 제목 ③`http(s)://` 로 시작하지 않는 url ④중복 url 을 전부 버리고, 버린 개수를 화면에 보고한다(테스트: 7행 중 2행만 통과).

**규칙도 AI 가 쓴다**: 타임라인을 만든 뒤 `keywords` 를 한 번 더 불러 수집 규칙을 세운다. 그래야 이후 30분 수집이 AI 없이도 이슈를 갱신한다.

**주인님 질문 — 기존 타임라인처럼 보이나: 확인했다. 그렇다.** 별도 화면이 아니라 같은 `openTrackerTimeline` 으로 들어간다. 날짜 카드 4개·레일·연결선 3개·클릭 가능한 링크·최신순 정렬·정렬 전환·기간 표시(`2026-02-01 ~ 2026-08-01 · 4일`) 전부 동작.

**진입점**: 트래커 페이지 `✦ AI로 이슈 추가`(키 있을 때만), 기사 시트 `✦ 이 뉴스를 AI로 추적 (과거까지)`. 기사에서 시작하면 그 기사 날짜가 시작일로 채워진다. 키가 없으면 AI 경로는 숨기고 기존 키워드 방식만 보인다.

**검증 335 PASS**: 타임라인 30(제공자별 검색 활성화·ChatGPT 엔드포인트 전환·지어낸 행 차단·정렬·시작일 유무별 프롬프트·빈 결과·오류) + 앱 38(입력 두 칸·미리보기·확인 후 생성·**타임라인 렌더 검증**·기사에서 시작 시 날짜 채움·키 없을 때 숨김) + 기존 267.

## 2026-08-28 — 과학 섬네일, 그리고 Google 차단 우회로를 찾음

주인님 지적 세 건. 셋째가 앞의 둘의 뿌리였다.

### 1. 과학 섬네일 0건 — 동아사이언스 추가

`동아사이언스` 는 **예외로 두기로 했던 매체인데 또 빠져 있었다.** `rss.donga.com/science.xml`,
실측 50건 중 48건이 `<media:content>` + 인라인 `<img>` 를 싣는다. 소스명을 `동아사이언스` 로
분리해 `DEFAULT_BLOCKED` 의 `동아일보` 와 안 겹치게 했다(차단은 정확일치 매칭).

**로컬 실측: 과학 이미지 0 → 47 / 120건.**

**왜 과학만 없었나** — 헬로디디·로봇신문은 같은 CMS 이고 `<description>` 이 순수 텍스트다.
`<media:*>`·`<enclosure>` 는 물론 인라인 `<img>` 조차 없다. `itemImage()` 는 정상이었고,
찾을 것이 없었을 뿐이다.

섬네일 있는 다른 한국 과학 피드는 **없다**(실측): `dongascience.com/rss.php` HTML 화,
`sciencetimes`·`kari.re.kr`·`etnews`·`zdnet`·`newsis science` 404 또는 item 0,
`mk.co.kr/rss/50300009/` 는 이미지 43건이지만 부동산·건설 섹션이라 과학이 아니다.

### 2. 네팔 홍수가 IT 에 뜬 이유

IT 가 **매일경제 `50100032` 하나에만** 의존한다. 그 피드는 IT 전용이 아니라 일반 기사가 섞인다.
라이브 49건 중 IT 무관이 최소 15건(네팔 홍수 2, 조선업, 파인애플, 학대아동 후원 2, 인사, 장학금…).
예전에는 Google 이 받쳐줘서 안 보이던 문제다.

### 3. 뿌리 — Google 이 Cloudflare 에서 전면 0건

라이브 피드(`/api/news`, `X-News-Source: kv`) 실측:

| | 건수 | Google 기여 |
|---|---|---|
| tech | 49 | **0** |
| science | 98 | **0** |
| (18개 전부) | — | **0** |

그런데 `error: none` 이다. `fetchCategory` 는 503 을 잡으면 `error` 를 채우는데 비어 있으니
**HTTP 200 + 본문에 `<item>` 없음** 이다. 08-19 에 "503" 으로 적어둔 것과 증상이 바뀌었고,
에러로도 안 잡혀 **조용히** 실패하고 있었다. 같은 URL 이 주인님 PC 에서는 정상(주요 34·IT 44·과학 70).

### 4. 우회로 — `feed2json.org` 가 뚫린다

08-19 에 "직접·릴레이 5개 전부 차단" 으로 결론냈던 건 **릴레이 후보가 달랐다.** 다시 훑었다:

| 릴레이 | 결과 |
|---|---|
| allorigins / codetabs / corsproxy | 실패 (예전과 동일) |
| r.jina.ai | 200 이지만 item 0 (마크다운으로 변환해버림) |
| **feed2json.org** | **5/5 성공, 평균 477ms** |
| **toptal feed2json** | **5/5 성공, 평균 101ms** |
| api.rss2json.com | 성공하나 무료 한도로 11건만 |

12개 카테고리 전수 실측 — **총 591건 회수**, 최신 항목이 실행 시각과 같다(08-27 23:38).

```
top 34  tech 60  science 70  business 70  world 36  nation 67
sports 36  ent 70  health 57  intl 38  intl_sci 53
```

**두 가지 함정**:

1. **`<source>` 태그가 사라진다.** JSON 변환기라 원본 XML 의 `<source url=...>` 이 없다.
   언론사 차단이 이것에 의존하므로 그냥 붙이면 차단이 통째로 무력화된다.
   → 제목 접미사 `" - 매체명"` 에서 복원 가능하다(**실측 591/591 전부 매칭**).
   `parseRss` 가 이미 그 접미사를 잘라내고 있어서, 자르기 전에 주워 담으면 된다.
2. **`when:1d` 연산자가 릴레이를 통과 못 한다.** 이중 인코딩(`%3A`)이 원인.
   `q=정치 when:1d` → 0건, `q=정치` → **104건**. 정치 카테고리만 이 형태를 쓴다.

첫 캐시 히트에서 3일 지난 사본이 오는 경우가 있었으나 재호출로 해소됐다(70건·당일).
수집은 30분 주기라 한 번 밀려도 다음 런이 메운다.

### 5. 그 밖에 확인한 것

**유튜브 쇼츠·영상은 붙일 수 있다.** `youtube.com/feeds/videos.xml?channel_id=<ID>` —
**키 불필요, 전 항목 `media:thumbnail` 포함**, 7개 채널 200. `link href` 가 `/shorts/` 인지
`/watch?v=` 인지로 쇼츠를 구분하고, `media:statistics views` 로 중요도를 매길 수 있다.
`parsePublisherRss` 가 이미 `<entry>` 와 `<link href>` 를 처리하므로 코드 수정은 거의 없다.
`?user=` / `?playlist_id=` 형식은 404 — `channel_id` 만 유효하다.

**IT 를 받칠 피드**(실측 200): `inews24.com/rss/news_it.xml` 100건,
`rss.etnews.com/Section901~902.xml` 각 30건, `hankyung.com/feed/it` 50건, `thelec.kr` 50건,
`aitimes.com` 50건, `epnc.co.kr` 50건. 전부 이미지는 없다.

**해외 과학 세분화 가능** — 소스가 이미 섹션별로 갈려 있다:
`phys.org/rss-feed/{space,earth,physics,biology}-news/` 각 30건·이미지 30,
`space.com/feeds/all` 50/150, `quantamagazine.org/feed/` 5/5,
`livescience.com/feeds/all` 50/150, `smithsonianmag.com/rss/science-nature/` 15/15.
국내 과학은 우주·지구과학 전용 RSS 가 없어(`kari.re.kr` 404, `astronomy.co.kr` DNS 없음) 세분화 불가.

### 6. 적용 (같은 날, 주인님 지시 '다 적용해')

**Google 릴레이** — `fetchGoogleFeed()` 가 `RELAYS` 두 곳을 순서대로 시도한다.
toptal(평균 101ms) → feed2json(477ms). JSON 을 RSS 모양으로 되돌려(`rssFromJsonFeed`)
`parseRss` 가 계속 유일한 파서로 남게 했다.
**0건은 실패로 센다** — 그게 바로 이번에 조용히 무너졌던 지점이라 성공으로 받으면 같은 버그가
새 자리에 생긴다.

**`<source>` 복원** — `splitTitleSource()`. 제목 뒤 `' - 매체'` 를 떼어 쓰되 30자 초과나
문장부호로 끝나면 제목의 일부로 본다. 실측 `nosrc` 0~1건/카테고리.
언론사 차단이 이 이름에 걸려 있어서, 못 살리면 차단이 무력화된다.

**`SLICE_COUNT` 2 → 3** — 세어보니 2 에서 가장 넓은 슬라이스가 **54 요청**(한도 50)이었다.
3 이면 41 로, 릴레이가 두 번째 호스트로 넘어갈 여유까지 남는다.
⚠️ **전체 갱신 주기가 60분 → 90분**이 된다(30분 cron × 3 슬라이스). 카테고리가 20개로
늘었으니 감수할 부분인데, 짧게 하려면 cron 을 20분으로 당기면 된다.

**소스별 상한 (`capPerSource`)** — 이게 없으면 피드를 추가할수록 사진이 줄어든다.
환경일보가 하루 38건을 쏟아 동아사이언스를 24건으로 밀어내며 **과학 사진이 47 → 22 로 떨어졌다**.
최신순 정렬만 있으면 다작 매체가 카테고리를 삼킨다.
- 소스당 20건, **사진·영상이 있으면 45건** (`MAX_PER_SOURCE_WITH_MEDIA`).
  과학은 사진 싣는 피드가 딱 하나라, 이 구분이 섬네일 유무를 가른다.
- 넘친 것은 버리지 않고 뒤에 붙인다 — 소스가 적은 카테고리도 120건을 채워야 한다.
- 결과: 과학 사진 **47건**(22 → 47), IT **30건**(24 → 30).

**IT 피더** — 아이뉴스24·전자신문(901/902)·한국경제·전자부품전문미디어·AI타임스 추가.
매일경제 단독 의존이 끝났다(네팔 홍수가 IT 에 뜬 원인).

**과학 피더** — 환경일보 추가. **의학신문·청년의사는 건강으로 보냈다** — 의약 전문지라
과학에 두면 임상·제약 기사가 과학을 덮는다.

**해외 과학 3분할** — `intl_science`(일반) + `intl_space`(우주) + `intl_earth`(지구·환경).
`orderedCategories()` 가 새 카테고리를 자동으로 뒤에 붙이고 기본 활성이라 앱 수정은 없었다.

**유튜브 영상·쇼츠** — 7개 채널. `parsePublisherRss` 가 이미 `<entry>`·`<link href>`·
`media:thumbnail` 을 처리해 파서는 그대로 두고, `link` 가 `/shorts/` 인지 `/watch?v=` 인지로
`video: 'short'|'video'` 만 붙였다.

⚠️ **채널 ID 를 처음에 6개 전부 틀렸다.** 핸들 페이지의 `"channelId"` 필드를 긁었는데 그것이
**사이드바 추천 채널까지 잡는다** — Kurzgesagt 대신 `Kurzgesagt After Dark`, PBS Space Time
대신 `PBS Documentaries`, 안될과학 대신 `안될공학` 이 들어왔다. 피드는 200 이고 항목도 15개라
**증상이 전혀 없었다**. `<link rel="canonical">` 에서 뽑고 피드의 `<author><name>` 으로
대조해 고쳤다. 채널을 추가할 때 이 절차를 반드시 지킬 것.

`clusterAndScore` 가 항목을 새로 만들어 담기 때문에 `video` 를 명시적으로 실어야 했다
(안 하면 수집은 되는데 화면까지 오지 않는다 — 실제로 첫 실행에서 `vid:0` 이었다).

**화면** — 섬네일에 재생 삼각형(`.thumb-video .play`, 이미지 파일 없이 border 로 그림),
메타 줄에 빨간 `영상`/`쇼츠` 배지. 래퍼 크기를 `.thumb` 과 같게 잡아 행 높이가 변하지 않는다.

**검증** — 로컬 전체 수집 **20/20 카테고리, 2385건, 사진 1464건, 영상 13건**.
Google 이 전 카테고리에 기여한다(예: 과학 pub 265 + google 70).
Edge headless 390px 렌더 확인 — 해외 우주 페이지에 `영상` 배지, 재생 삼각형,
일반 기사 섬네일에는 오버레이 없음.

**아직 안 한 것**: 배포. `npx wrangler deploy` + Pages 배포는 지시받고 한다.
배포 후 `/run?key=<CRON_SECRET>&only=news` 를 **슬라이스 3번** 돌려야 첫 발행이 된다.

### 7. 배포 — 카테고리를 늘리다 피드를 세 번 깨뜨림

카테고리를 18 → 20 으로 늘린 것이 **KV 에 이미 저장돼 있던 슬라이스와 충돌**했다.
슬라이스는 각자 저장될 당시의 코드가 쓴 것이라, 카테고리 목록이 바뀌면 서로 다른 세계를
담고 있게 된다. 이걸 못 보고 세 번 연속으로 반쪽짜리 수정을 했다.

| 시각(UTC) | 증상 | 원인 |
|---|---|---|
| 01:00 | 카테고리 24개, 6개 중복. 과학이 사진 0인 옛 사본으로 표시 | `merged.push(cat)` 가 id 확인 없이 이어붙임 |
| 01:30 | 17개로 줄고 **과학·해외·해외 지구·환경 사라짐** | 중복은 고쳤으나 그 셋이 든 슬라이스가 아직 구버전 |
| 01:40~02:00 | 슬라이스는 저장되는데 발행이 안 됨 | 슬라이스가 RSS 원본 XML 통째 저장(3개 합 9.5MB) |
| **02:10** | **20개·중복 0·과학 사진 41건** ✅ | 이미지행만 저장으로 전환(9.5 → 1.45MB) |

**고친 것 셋**:
1. `worker/cron.js` 병합을 **id 키 + 최신 슬라이스 승 + 선언 순서 정렬**로. 코드에서 지운
   카테고리가 KV 에 남아 계속 나오는 것도 같이 걸러진다.
2. cron `*/30` → `*/10` (전체 갱신 90분 → 30분). ⚠️ **슬라이스 선택 나눗셈도 같이**
   고쳐야 했다 — `floor(now/30분) % 3` 을 그대로 두면 10분 간격에서 **연속 3번이 같은
   슬라이스**를 수집한다(`2,0,0,0,1,1,1,2,2`). `CRON_PERIOD_MS` 상수로 묶었다.
3. 슬라이스가 **XML 대신 뽑아낸 title→image 쌍만** 저장. 원본은 색인 만들려고 두던 것인데,
   매 실행 9.5MB 를 읽고 재파싱해서 슬라이스당 600쌍을 다시 만들고 있었다.

**KV 쓰기 한도 확인** (`*/1` 로 하면 안 되는 이유): 한 번 실행에 2회 쓰기(슬라이스+발행본),
무료 1,000/일. `*/30`=96 · **`*/10`=288** · `*/5`=576 · `*/1`=2,880(초과). 게다가 1분마다
언론사 76곳을 두드리면 차단당하고, RSS 는 그렇게 자주 안 바뀌어 실익도 없다.

**`/status` 추가** — `https://news-feeder-cron.jayhouse2001.workers.dev/status`.
읽기 전용이라 `CRON_SECRET` 불필요. 마지막 실행·슬라이스별 수집 시각·발행 시각·
카테고리 수·**중복 수**·카테고리별 사진/영상 건수를 준다. 이번 추적이 전부 이걸로 가능했다.

**⚠️ 남은 문제 — 실패한 실행이 흔적을 안 남긴다.** 상태 기록(`STATUS_KEY`)이 병합·발행
**다음**에 있어서, 중간에 죽으면 `lastRun` 이 이전 성공 시각 그대로다. 그래서 01:40~02:00
구간을 보고 **"cron 이 아예 안 돈다" 고 오진했다** — 실제로는 돌다가 죽고 있었다.
다음에 같은 일이 생기면 또 추측하게 된다. 수집 시작 시점에 먼저 한 줄 남기고 끝나면
덮어쓰는 식으로 고쳐야 한다. (주인님 판단 대기)

**교훈**: `SLICE_COUNT` 를 올릴 때 **요청 수만 세고 저장 크기를 안 봤다.** 슬라이스는
요청 예산만이 아니라 KV 저장·재파싱 비용도 함께 늘린다. 그리고 **KV 에 남아있는 옛
슬라이스는 코드 변경과 별개로 살아있다** — 카테고리·저장 형식을 바꿀 때는 항상
"이미 저장된 것과 섞이면 어떻게 되나" 를 먼저 봐야 한다.

### 8. 과학 카테고리가 과학이 아니었다

주인님 지적: "이게 과학이냐?" — 세어보니 **120건 중 70건이 과학이 아니었다.**

**소스 선정이 틀렸다.** 매체 이름만 보고 골랐다:

| 소스 | 비과학 | 실체 |
|---|---|---|
| 환경일보 | 21건 중 **19건** | 환경 매체가 아니라 **지자체 홍보 와이어**. 예천군 구제역, 양산시 추경, 성남시의회 개원식 |
| 동아사이언스 | 44건 중 **28건** | `rss.donga.com/science.xml` 이 실은 **동아일보 IT면**. 엔비디아 실적이 금성 연구와 같이 온다 |
| 로봇신문 | 20건 중 **18건** | 로봇/AI **산업지**. 과학지가 아니다 |
| 헬로디디 | 26건 중 **10건** | 연구계 **정책·투자** 매체 |

→ 환경일보는 제거. 나머지 셋은 사진·기사 공급원이라 유지하되 제목으로 거른다.

**빼는 방식(deny only)은 실패했다.** 지자체·주가 패턴을 걸러도 남은 것이 여전히 AI 제품
출시·폰 리뷰·소송이었다. 그 매체들이 원래 그런 기사를 쓰기 때문이다.

**뒤집었다 — 통과 조건 + 배제 조건 둘 다.**
- `SCIENCE_SUBJECT`: 우주·유전자·핵융합·연구팀 등 **과학 주제어가 있어야** 통과
- `TRADE_STORY`: 출시·수주·소송·사업자·장관 등 **산업/정책어가 있으면** 제외

둘 다 필요하다. `AI가 설계하는 자율실험실`(과학) 과 `모두의 AI 사업자 선정`(아님)은
**두 번째 조건이 있어야** 갈린다. 허용목록만 쓰면 "금성 무늬" 같은 진짜 과학이 탈락하고,
배제목록만 쓰면 산업 기사가 그대로 남는다.

**결과: 과학 120 → 79건, 전부 실제 과학.** 79건이 하루치 국내 과학 기사의 실제 분량이다.

### 9. 국내 과학 세분화는 철회

우주/지구·환경/물리·화학/생명과학 4개를 Google 검색으로 만들었다가 **뺐다.**
검색은 질의에 답할 뿐 주제에 답하지 않는다 — `우주 OR 위성` 이 박물관 프로모션과
대학 입시요강을, `양자` 가 주식 테마와 코인 보도자료를 물어왔다. 키워드 필터로
스팸은 걷혔지만 남은 것도 대부분 채우기용이었다(우주 사진 4건, 물리 1건).

**자기 주제로 채울 수 없는 카테고리는 만들지 않는다.** 국내는 `과학` 하나로 되돌렸다.
해외는 phys.org·NASA·Quanta 등 **실제 분야별 데스크 피드**가 있어서 유지
(해외 과학/우주/지구·환경/**물리**). 물리는 앞서 넣는다고 해놓고 빠뜨렸던 것을 추가했다.

**최종 21개 카테고리.** 슬라이스 4 · cron `*/5` (전체 갱신 20분, KV 쓰기 576/1000).

## 2026-08-31 — 해외 기사 제목이 리스트에서 번역되지 않던 문제 (v1.0.2)

기사를 열면 번역이 되는데 **리스트의 제목만** 영어로 남아 있었다. 기다려도 바뀌지 않았다.

### 원인은 엔드포인트가 아니라 요청량이었다

`client=gtx` 는 실측 상시 **429**(`automated queries`). 같은 호스트의 `client=at` 은
200 + `Access-Control-Allow-Origin: *` 로 정상이라 처음엔 엔드포인트 교체로 끝난 줄 알았다.
**틀렸다.** `at` 으로 15연타를 넣자 `at`·`gtx` 가 **둘 다** 429 로 막혔다 —
두 client 는 **같은 per-IP 쿼터를 공유**한다. 엔드포인트를 바꿔도 한도는 그대로다.

진짜 원인은 한 번에 쏘는 제목 수다.

| | 값 |
|---|---|
| 해외 카테고리 | 11개 |
| `rebuildPages()` 가 **미리 만드는** 페이지 | 11개 전부 |
| 페이지당 초기 렌더 (`PAGE_STEP`) | 15건 |
| **초기 로드가 요청하던 제목** | **165건** (화면에 보이는 건 15건) |
| 요청 간격 | 없음 |

`translateNow()` 가 `sweep(document)` 로 **문서 전체**를 훑기 때문에, 화면 밖 10개
페이지의 제목까지 전부 요청했다. 165건을 쉬지 않고 쏘면 Google 봇 탐지에 걸린다.

### 실패가 영구적이었던 이유

`sweep()` 이 fetch **전에** `data-tr` 을 먼저 지웠다. 429 로 실패하면 표시가 이미 없어
**다음 sweep 이 두 번 다시 시도하지 않는다.** "아무리 기다려도 안 되던" 것이 이것이다.

### 수정

- **보이는 페이지만 번역한다** (`onScreen()`). 화면 밖 페이지는 `data-tr` 을 남긴 채 건너뛴다.
- **스와이프가 멈추면 그 페이지를 번역한다** (`translateVisibleSoon()`, 250ms 디바운스).
  `syncPageIndicator()` 는 스와이프 중 매 프레임 도므로 디바운스가 없으면 훑고 지나간
  페이지까지 전부 요청한다.
- **더보기**는 기존 `translatePending(ul)` 이 새로 붙은 항목만 처리한다 (변경 없음).
- **배치 10건**(`&q=` 반복 — 응답이 제목당 한 그룹) + **배치 간 0.9초**.
- **429 면 10분 백오프**(`trBlockedUntil`), 그리고 `data-tr` 을 **남긴다** → 풀리면 재시도.
- 배치 안 **중복 제목 합침** — 같은 헤드라인이 여러 카테고리에 뜬다.
- `client=gtx` → `client=at`.

### 검증 (DOM 스텁, 브라우저 없이)

`onScreen`/`translateNow`/`gtxBatch` 를 그대로 떼어내 11페이지×15건 상황을 재현했다.

```
초기 로드 (1페이지만 보임)  요청 2회 [10,5]   ← 기존 165회
                            1페이지 15/15 번역, 나머지 150건은 표시 유지
2페이지로 스와이프          추가 2회, 15/15
더보기 15건                 추가 2회, 15/15
같은 제목 10개              요청 1회 (중복 합침)
429 차단 중                 요청 0회, 15건 표시 유지 → 풀린 뒤 15/15 성공
```

### 남은 미검증 1가지

**다중 `q=` 의 실제 응답 형식은 실측하지 못했다.** 테스트 버스트로 이 PC IP 가
429 에 걸려 있었기 때문이다. 파서는 단일 `q=` 실제 응답으로만 검증했다
(긴 제목이 문장별로 쪼개져 오는 경우 포함). 형식이 다르면 `shape` 로 던져
`untranslated` 가 되고 표시는 남으므로 **원문이 보일 뿐 앱이 깨지지는 않는다.**
차단이 풀리면 실측할 것.

### 주의

- **`client` 를 바꿔도 한도는 늘지 않는다.** 같은 IP 쿼터다. 번역이 막히면
  엔드포인트를 갈아끼우지 말고 **요청 수를 줄여라.**
- **테스트로 번역 API 를 연타하지 말 것.** 15회로 IP 가 수십 분 막혔다.

## 2026-08-31 — 번역 제공자 이중화: Google + MyMemory 폴백 (v1.0.3)

Google 하나에 매달린 구조라 429 가 뜨면 번역이 통째로 멈췄다. 무료·키 불필요·CORS
허용인 후보를 전부 실제로 호출해 보고 폴백을 붙였다.

### 후보 실측 (2026-08-31)

| 후보 | 결과 | 키 | CORS | 무료 한도 |
|---|---|---|---|---|
| **MyMemory** | **200, 번역 정상** | 불필요 | `*` | 5천자/일(익명), 5만자/일(`de=`이메일) |
| Google `client=at` | 429 (당시 차단 중) | 불필요 | `*` | 비공개 |
| Lingva (미러 6곳) | **전멸** 500/403/503 | 불필요 | — | — |
| SimplyTranslate | 500 | 불필요 | — | — |
| LibreTranslate 공개 | 400 (키 요구) | 필요 | — | — |
| MS Translator / DeepL | 미채택 | **필요** | 키 노출 | 200만자/월 / 50만자/월 |

**"Lingva 는 무제한"이라는 2차 자료는 믿을 수 없다** — 미러 6곳이 전부 죽어 있었다.
정적 PWA 라 **키가 필요한 제공자는 쓸 수 없다**(브라우저에 키가 노출된다).
서버를 거치면 Worker 요청 한도 50개에 걸린다.

### MyMemory 실측

- **연타 제한 없음**: 12연타 12/12 성공. Google 은 15연타에 IP 차단.
- **배치 없음**: 제목당 요청 1회. `&q=` 반복이 안 된다.
- **`langpair` 고정 필요**: `auto` 를 안 받는다. 해외 카테고리 11개가 전부 `lang:'en'`
  이라 `en|ko` 로 박았다.
- 351자 제목(실측 최장)도 정상. 한도는 500바이트.
- 한도 초과·거부는 HTTP 가 아니라 **본문 `responseStatus`** 로 온다 — `res.ok` 만 보면
  놓친다. `quotaFinished` 도 같이 본다.

**한도가 빠듯하다.** 제목 평균 75자 → 화면 1페이지(15건)에 1,125자.
익명 5천자면 **하루 4페이지**. 캐시가 있어 실사용은 버티지만 주력으로 쓸 수는 없다.

**품질은 Google 보다 한 수 아래다.** 실측:
```
Judge Once Again Bars Postal Service...  ->  "Judge Once Again은 우편 서비스..."  (사람 이름으로 오인)
Trump Administration's Blacklisting of Anthropic  ->  "인류학 블랙리스트"        (Anthropic 오역)
```

### 구조

**Google 1순위, MyMemory 폴백.** 교체가 아니라 추가다.

- Google 429 → `trBlockedUntil` 10분 park → 그 동안 MyMemory
- MyMemory 한도 소진 → `mmBlockedUntil` 1시간 park → 둘 다 죽으면 throw,
  `data-tr` 이 남아 나중에 재시도
- **`shape`·네트워크 오류는 폴백하지 않고 그대로 던진다.** 그건 Google 이 죽은 게
  아니라 이쪽 코드 버그이고, 조용히 MyMemory 할당량만 태우면 원인이 가려진다.
  429 만 `e.rateLimited` 로 표시해 구분한다.
- 익명 사용 — `de=` 에 이메일을 넣지 않았다(주인님 주소를 쓰는 문제라 보류).

### 검증

폴백 분기 7가지 (스텁):
```
1) Google 정상          google=1 mymemory=0
2) Google 429           -> MyMemory 로 전환, 3건 번역
3) park 중 재호출       google 추가호출 0 (10분간 안 두드림)
4) 둘 다 죽음           throw, 표시 유지
5) MyMemory 한도 park   mymemory 추가호출 0 (1시간)
6) shape 오류           throw, mymemory=0  ← 폴백에 안 먹힘
7) park 만료            Google 재시도 1회
```

**실네트워크 end-to-end**: Google 이 실제로 429 인 상태에서 실기사 제목 5건
→ MyMemory 경유 **5/5 번역 성공**.

여전히 **다중 `q=` 실제 응답 형식은 미검증**이다(Google 이 계속 429 라 확인 못 함).
틀리면 `shape` 로 던져 원문이 보일 뿐 앱은 깨지지 않는다.
