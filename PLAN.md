# 뉴스 피드 웹서비스 계획

iPhone 에서 보는 개인 뉴스 피드. GitHub public repo 하나로 호스팅 + 데이터 수집까지 해결.

## 실현 가능성 확인 결과 (2026-08-05 검증)

| 항목 | 결과 |
|---|---|
| Google 뉴스 RSS (한국어, 전체 + 카테고리별) | ✅ 동작 확인. 항목마다 `<source>` 태그로 언론사 식별 가능 → 언론사 차단 기능 구현 가능 |
| Open-Meteo 날씨 API | ✅ 동작 확인. 무료·API 키 불필요·CORS 허용 → 브라우저에서 직접 호출 가능 |
| GitHub Pages | ✅ public repo 무료, HTTPS 기본 → iPhone Safari + 홈 화면 PWA 가능 |
| GitHub Actions cron | ✅ public repo 무료. RSS 수집을 서버 측에서 수행 (브라우저 CORS 문제 우회) |
| 개별 언론사 RSS (한겨레·매경 등) | ⚠️ 이 세션의 fetch 제한으로 미검증. Actions 러너에서는 가능할 확률 높음 — 구현 단계에서 검증 후 소스 추가 |

## 아키텍처

```
[GitHub Actions cron (30분마다)]
    └─ RSS 수집 스크립트 (Node.js) → 정규화 → data/news.json 생성 → Pages 배포
[GitHub Pages (정적 사이트, PWA)]
    ├─ news.json 을 fetch 해서 렌더링
    ├─ 사용자 설정(카테고리·차단·정렬)은 localStorage
    └─ 날씨는 브라우저에서 Open-Meteo 직접 호출
```

- 서버 없음. repo 하나로 수집·호스팅 완결.
- 브라우저가 RSS 를 직접 못 읽는 CORS 문제는 Actions 가 대신 수집해 JSON 으로 두는 방식으로 우회 (검증된 표준 패턴).

## UI 구성 (2026-08-05 개정 — 좌우 스와이프 페이저)

초기 와이어프레임(하단 탭 3개): https://claude.ai/code/artifact/fb51d19f-a963-451f-b8a3-286f11d0067d
→ **폐기.** 주인님 지시로 아래 구조로 변경.

- **하단 탭바 없음.**
- **좌우 스와이프로 페이지 전환**: `[대시보드] → [카테고리1] → [카테고리2] → …` (CSS scroll-snap).
  - 카테고리 순서·표시 여부 = 설정의 카테고리 목록이 그대로 페이지 순서가 됨.
- **헤더**: 왼쪽에 현재 페이지 제목, 오른쪽에 갱신 시각 + **⚙ 설정 아이콘**.
- **헤더 아래 칩 줄**: 페이지 인디케이터 겸 바로 이동 (현재 페이지 강조, 탭하면 해당 페이지로 스크롤).
- **대시보드 페이지**: 위젯 스택 (날씨·미세먼지 → 중요 뉴스 → 바로가기) + "＋ 위젯 추가".
- **카테고리 페이지**: 정렬 선택 + 건수 → 기사 리스트. 기사 ⋯ → 액션 시트(언론사/키워드 차단).
- **날씨**: **휴대폰 위치를 자동으로 읽어** 그 위치의 현재 날씨 + **주간(7일) 예보**. 위치 거부/실패 시 마지막 위치 또는 서울로 폴백.
- **설정 패널**(⚙): 카테고리 표시·순서 / 차단 언론사 / 차단 키워드 / 선호 언론사.

## 기능 설계

### 1. 뉴스 카테고리
- Google 뉴스 topic 피드 사용: 주요 / 정치 / 경제 / IT / 과학 / 세계 / 사회 / 스포츠 / 연예 / 건강 (10개)
- 카테고리별 탭 UI. 어떤 카테고리를 켤지 설정에서 선택 (localStorage).

### 2. 차단 (block)
- **언론사 차단**: RSS `<source>` 의 언론사명/도메인 기준. 기사 옆 버튼으로 즉시 차단 추가.
- **키워드 차단**: 제목에 포함된 단어 기준 (피드 단위 제외 요구 대응).
- 차단 목록 관리 화면 (추가/삭제). localStorage 저장.

### 3. 카테고리별 정렬
- 카테고리마다 정렬 기준 선택: 최신순 / 중요도순(클러스터 크기) / 언론사 우선순위순.
- 카테고리 탭 자체의 순서도 드래그(또는 위/아래 버튼)로 변경.

### 4. 대시보드 (첫 화면) — 위젯 방식
- 대시보드는 **위젯의 모음**. 위젯을 추가/삭제/순서 변경할 수 있고, 위젯마다 자체 설정을 가짐 (전부 localStorage).
- 위젯은 공통 인터페이스(등록 → 렌더 → 설정 화면)를 갖는 모듈로 만들어, 나중에 새 위젯을 쉽게 추가할 수 있는 구조.
- **초기 제공 위젯**:
  - **중요 뉴스**: 중요도 스코어 상위 N 건. 어떤 카테고리를 포함할지·몇 건 보일지 위젯 설정에서 선택.
    - 중요도 스코어(수집 단계 계산): 제목 유사도 클러스터링 → 여러 언론사가 동시에 다룬 이슈일수록 높은 점수 + 최신성 가중치.
  - **날씨**: Open-Meteo `forecast` API (✅ 검증). 현재 기온·날씨 코드·강수 + 주간 최고/최저·강수확률. 위치는 기본 서울 + geolocation 옵션.
  - **미세먼지**: Open-Meteo Air Quality API (✅ 검증, 키 불필요). 현재 PM10/PM2.5 + 등급(좋음/보통/나쁨) 표시. 날씨와 같은 위치 설정 공유.
  - **바로가기 링크**: 사용자가 링크(이름 + URL)를 직접 **추가/삭제/편집**. 웹 링크 외에 iPhone URL scheme (`tel:`, `sms:`, `kakaotalk://`, `shortcuts://` 등) 으로 앱 실행 가능. localStorage 저장.
- **추가 후보 위젯** (주인님 선택):
  - **환율**: Frankfurter API (✅ 검증, 키 불필요). USD/JPY/EUR → KRW 등. ECB 고시 기준 1일 1회 갱신.
  - **관심 키워드 뉴스**: 등록한 키워드(예: "환율", "전기차")가 제목에 들어간 기사만 모아 보여줌. 수집된 데이터 재활용이라 구현 쉬움.
  - **외신 헤드라인(번역)**: 외국 뉴스 번역 기능(하단 참조)과 연동.
  - **카테고리 뉴스**: 특정 카테고리 최신 N 건을 대시보드에 고정.
  - **나중에 읽기**: 기사에 북마크 버튼 → 모아 보기 (localStorage).
  - 주가지수(KOSPI/나스닥)는 안정적인 무료 공식 API 가 마땅치 않아 보류 (비공식 소스뿐).

### 5. 외국 뉴스 번역 (추가 요구)
- 수집 단계에서 외국 피드(Google 뉴스 영어판, BBC 등)의 제목·요약만 번역해 원문과 함께 `news.json` 에 저장. 번역 캐시로 새 기사만 번역.
- 번역 수단 미정: ① Google 번역 비공식 엔드포인트(무료·키 불필요·비공식) ② DeepL API Free(공식·월 50만 자·키 필요) — **주인님 선택 대기**.
- 기사 본문은 원문 링크로 열고 iPhone Safari 자체 번역 사용.

### 6. iPhone 대응
- 모바일 우선 반응형 레이아웃.
- PWA: manifest + 아이콘 → Safari "홈 화면에 추가" 로 앱처럼 사용.

## 기술 스택 (단순 우선)

- **프론트**: 순수 HTML/CSS/JS (빌드 스텝 없음). 프레임워크 불필요한 규모.
- **수집**: Node.js + `rss-parser`, GitHub Actions cron.
- **배포**: GitHub Pages (Actions 의 pages 배포 액션).

## 단계별 마일스톤

1. **M1 수집 파이프라인**: repo 구조, 수집 스크립트, Actions cron → `news.json` 생성 확인
2. **M2 기본 뷰어**: 카테고리 탭 + 기사 리스트 (모바일 레이아웃), Pages 배포 → iPhone 에서 열기
3. **M3 차단 기능**: 언론사/키워드 차단 + 관리 화면
4. **M4 정렬**: 카테고리별 정렬 기준 + 카테고리 순서 변경
5. **M5 대시보드 (위젯 프레임)**: 위젯 등록/추가/삭제/순서변경 구조 + 중요 뉴스 위젯
6. **M6 날씨·미세먼지 위젯**: Open-Meteo forecast + air-quality
7. **M7 PWA 마무리**: manifest·아이콘·홈 화면 추가 안내
8. **M8 (선택) 확장**: 개별 언론사 RSS 추가, 외국 뉴스 번역, 추가 위젯(환율/키워드/북마크 등)

## 한계 (미리 알아둘 것)

- **실시간 아님**: Actions cron 은 5~15분 지연이 흔함. 30분 주기면 체감 문제 없음.
- **설정은 기기별**: localStorage 라 iPhone/PC 간 동기화 안 됨. 필요해지면 설정 export/import(JSON 복붙) 또는 Supabase 연동으로 확장 가능.
- **public repo**: 코드·수집 데이터 공개됨. 개인 설정은 repo 에 안 올라가므로 문제 없음.
- 기사 본문은 저작권상 저장하지 않고 제목+요약+원문 링크만.
