# AGENTS — 작업 지침 및 할 일

이 repo 에서 작업하는 AI 에이전트용. 계획은 [PLAN.md](PLAN.md), 완료 기록은 [WORK_NOTE.md](WORK_NOTE.md).

## 규칙

- 지시받은 범위만 작업한다. `git commit`/`push` 는 주인님이 명시적으로 시킬 때만.
- 각 마일스톤 완료 시 WORK_NOTE.md 에 날짜·내용 기록.
- 기사 본문은 저장하지 않는다 (제목·요약·링크만).
- 프론트는 빌드 스텝 없는 순수 HTML/CSS/JS 유지 — 프레임워크 도입은 별도 승인 필요.
- UI 는 [PLAN.md](PLAN.md) 의 "UI 구성" 절을 따른다 (하단 탭바 4개, 좌우 스와이프 페이저, 헤더 ⚙, 위젯 대시보드). 구조 변경은 주인님 승인 필요.
- **하단 탭바 = 섹션 전환, `pagedots` = 뉴스 탭 안의 카테고리.** 스와이프는 뉴스 탭 안에서만 동작한다. 새 섹션은 `TABS` + `tabPages()` 에 추가.
- **트래커 자동 수집에 AI 를 붙이지 않는다.** 제목 부분일치(AND/OR 키워드) + Google 뉴스/GDELT 검색으로만 수집한다. 앱이 외부 AI API 를 호출하는 구조는 별도 승인 필요 — 현재 AI 연동은 **사용자가 직접 복사·붙여넣기** 하는 방식뿐이다.
- **Worker 한 실행은 외부 요청 50개까지다.** 수집을 카테고리 2개 슬라이스로 나눈 이유가 이것(각 37/32 요청). 슬라이스를 합치거나 피드를 늘릴 때 이 한도를 넘기지 말 것 — 넘으면 나머지가 전부 실패하고 `all categories failed` 가 된다. 로컬 Node 에는 제한이 없어 테스트로는 안 잡힌다.
- **같은 피드를 두 번 부르지 말 것.** 이미지 색인과 기사 수집이 같은 URL 을 각각 받아 요청이 두 배였다(2026-08-19). 한 번 받아 `parsePublisherRss` 와 `parseImageFeed` 양쪽에 넘긴다.
- **슬라이스는 전부 모였을 때만 발행한다.** 하나씩 발행하면 앱이 반쪽 피드를 본다. 미완성일 때는 KV 의 이전 발행분이 그대로 서빙된다.
- **모든 cron 실행은 `cron:last` 에 결과를 남긴다.** 실패한 실행은 아무것도 쓰지 않아 "trigger 미등록"과 구분이 안 됐다(2026-08-19, 40분 낭비). 이 기록을 제거하지 말 것.
- **수집은 Cloudflare cron Worker 한 곳에서만 한다** (2026-08-19 이전 완료). GitHub Actions 는 배포 전용이고 cron 이 없다. 수집 로직은 `shared/collect-news.js` 에 런타임 중립으로 두어 Node 스크립트와 Worker 가 같은 코드를 쓴다 — 한쪽만 고치는 일이 생기지 않게.
- **`site/data/news.json` 을 배포에서 빼지 말 것.** 새 배포 직후 cron 이 아직 안 돌았거나 KV 바인딩이 없을 때 앱이 비어 보이는 것을 막는 fallback 이다. `loadNews()` 는 `api/news` → 파일 순으로 시도한다.
- **뉴스는 KV, 계정·트래커는 D1.** 뉴스는 950KB 를 페이지 로드마다 통째로 읽으므로 행 단위 질의를 하는 D1 은 맞지 않는다.
- **HTTP 헤더 값에 한글을 넣지 말 것.** 헤더는 Latin-1 이라 `Response` 생성 시점에 `TypeError` 가 난다(2026-08-19 `X-News-Source` 에서 실제 발생). 진단용 문자열은 ascii 로.
- **모든 카테고리는 언론사 피드를 최소 하나 가져야 한다.** Google 단독 카테고리는 서버에서 503 을 맞으면 통째로 빈 화면이 된다(2026-08-20 과학). 사진이 없는 카테고리가 기사가 없는 카테고리보다 낫다.
- **소급 큐는 최신 창부터 처리한다(`queue.reverse()`).** 2년 구간은 창 25개인데 요청 한도는 24이고, 사건이 터진 달은 쪼개져 예산을 더 먹는다. 오래된 것부터 훑으면 정작 보려는 최근 구간에 도달하지 못한다.
- **로그인 상태에서도 과거 소급은 브라우저가 한다.** 서버는 Google 을 못 부르므로 `/trackers/:id/update` 는 수집하지 않고 `clientSweep` 만 알린다. 클라이언트가 찾아서 `uploadEvents()` 로 올린다.
- **모델명·API 스펙은 반드시 공식 문서를 읽고 적는다.** 기억으로 쓴 모델명 3개가 전부 틀렸고(2026-08-20), "OpenAI·Grok 은 웹 검색이 안 된다"고 단정한 것도 틀렸다. 넷 다 된다. 틀린 모델명은 사용자가 키를 넣고 눌러야 드러난다.
- **AI 가 만든 타임라인 항목은 링크를 검증하고 넣는다.** `http(s)://` 로 시작하지 않거나 날짜 형식이 아니면 버린다 — 모델은 형식만 채우려 들고, 가짜 링크는 열리지 않는 항목이 된다. 버린 개수는 화면에 보고한다.
- **AI 로 이슈를 만들 때 수집 규칙도 함께 세운다.** 그래야 이후 30분 수집이 AI 없이 이슈를 갱신한다. 타임라인만 만들고 규칙을 비워두면 그 이슈는 그 시점에 멈춘다.
- **웹 검색은 제공자마다 켜는 법이 다르고 ChatGPT 만 엔드포인트가 바뀐다**(`/v1/responses`). 하나로 통일하려 하지 말 것.
- **AI 키는 절대 저장하지 않는다.** `/api/ai` 는 받아서 한 번 쓰고 버린다 — D1 에 쓰지도, 로그에 남기지도 않는다. 키는 사용자 `localStorage` 에만 있고, 브라우저가 네 제공자를 직접 못 부르기 때문에(CORS) 서버를 거칠 뿐이다. 계정 불필요(`plainHandler`).
- **AI 는 전부 선택이다.** 키가 없으면 앱은 지금까지처럼 동작해야 하고, AI 화면은 오류가 아니라 설정 안내를 보여준다.
- **AI 응답에서 JSON 은 `extractJson` 으로 뽑는다.** 형식을 못 박아도 모델은 앞뒤에 말을 붙인다. `JSON.parse(text)` 를 직접 부르지 말 것.
- **AI 가 지울 항목은 먼저 보여주고 확인을 받는다.** 모델 판단으로 사용자 데이터를 말없이 지우지 않는다. 삭제는 `removeEvent` 를 거쳐야 다음 수집이 되살리지 않는다.
- **키워드는 경고문이 아니라 숫자로 보여준다.** "흔한 단어를 피하라"는 행동으로 옮길 수 없다. 문서빈도 배지 + 실시간 매칭 건수 + 제목 미리보기로 대신한다(실측: `미국` 은 12건 중 1건만 관련).
- **트래커 수집은 두 갈래다. 합치려 하지 말 것.** *과거 히스토리*는 검색이 필요하므로 브라우저에서(`sweep.js`, Google 뉴스) — 서버는 503 으로 막힌다. *새 기사*는 검색이 불필요하므로 서버에서(`match.js`), cron 이 이미 받아둔 뉴스 기사에 키워드를 대본다. 후자는 **추가 요청 0** 이고, 요청 한도가 50인 환경에서 이게 핵심이다.
- **소급은 브라우저에서 직접 호출한다.** 트래커가 localStorage 에 있어 Actions 는 이슈 목록을 볼 수 없다. GDELT 는 rate limit 이 심하니 요청 간격 5.5초를 줄이지 말 것.
- **자동 수집은 이미 자리 잡은 날짜를 건드리지 않는다(`settled`).** 지난주 기사가 새로 나와도 지난주에 일어난 일은 바뀌지 않고, 읽던 날이 조용히 다시 쓰이는 것은 기사가 빠지는 것보다 나쁘다. **단 과거 소급과 수동 추가는 예외** — 전자는 옛 날짜를 채우는 게 목적이고, 후자는 사용자의 선택이다.
- **하루 상한은 수집이 아니라 표시 단계다.** 기사는 다 모으고 `DAY_PREVIEW` 만큼만 보여준 뒤 나머지를 접는다. 수집 단계에서 버리면 더보기를 만들 수 없고 숫자를 바꿀 때마다 재수집해야 한다.
- **`BLOCKED_SOURCES` 는 `allSources` 로 못 끈다.** 그 스위치는 주요 매체 목록을 넓히는 것이고, 뉴스 매체가 아닌 곳(게임·도박 글, 자동번역 농장)을 통과시키는 뜻이 아니다. 목록은 `tracker.js` 한 곳에 두고 세 소비자가 공유한다.
- **펼친 날짜 상태는 이슈 id 에 묶는다.** 타임라인은 정렬·삭제·새로고침마다 새로 그려지므로 전역으로 두면 다른 이슈가 물려받는다.
- **중복 판정은 url 이 아니라 "같은 날 + 제목 유사도"다.** 같은 사건이 30분 피드·Google 소급·수동 추가 세 경로로 들어오는데 경로마다 url 이 달라서, url 비교만으로는 통신사 원문과 재작성본이 둘 다 통과한다. `collectTrackers()` 와 `runBackfill()` **양쪽 모두** `dedupIndex()`/`isDuplicate()` 를 쓴다 — 한쪽만 고치지 말 것.
- **키워드 점수의 문서빈도 기준은 절대 건수다 — 비율로 바꾸지 말 것.** `서울` 은 2016건 중 26건(1.3%)이라 어떤 백분율 기준도 통과하지만, 추적 키워드로는 너무 넓다. 말뭉치 크기와 무관하게 `호르무즈`(3건)와 `서울`(26건)을 가르는 건 건수뿐이다.
- **희귀도만으로 키워드를 고르지 말 것.** 실측: 문서빈도 낮은 순으로 뽑으면 `완전`·`부인` 이 `트럼프`·`이란` 을 이긴다. 평균 빈도(4.3→2.0)는 좋아지는데 결과는 못 쓴다. `WEAK_WORDS` 배제 + 형태(라틴 문자)·위치 가점을 함께 써야 한다.
- **등록 화면은 제목의 모든 단어를 칩으로 제공하고 상위 2개만 켜둔다.** 어떤 단어가 사건을 지칭하는지는 사람이 즉시 아는 것이고 점수는 추측이다. 순위는 기본값만 정하지 선택지를 제한하지 않는다.
- **`suggestKeywords()` 는 조사·어미를 떼고 명사만 남긴다.** 형태소 분석기는 쓰지 않는다(제목 부분일치라 어간만 남으면 충분). 어미(`VERB_TAIL`)를 거르는 이유는 `발표했다` 같은 말이 "무엇에 관한 이슈인지"를 말해주지 않아 무관한 기사를 끌어오기 때문이다.
- **앱은 계정 없이도 완전히 동작해야 한다.** 로그인은 선택이다. `/api/*` 가 배포되지 않은 환경에서도 뉴스·트래커·백업이 전부 되어야 하고, 계정 확인 실패는 조용히 로그아웃 상태로 떨어진다. 첫 페인트를 API 응답에 묶지 말 것.
- **어느 트래커 목록을 쓸지는 `trackerList()` 한 곳에서만 정한다.** 로그인 상태면 `remoteTrackers`, 아니면 `S.trackers`. 서버 사본을 `S` 에 쓰지 말 것 — 로그아웃 시 기기 데이터가 서버 것으로 오염된다.
- **트래커 API 는 모든 쿼리에서 SQL 단계로 `user_id` 를 건다.** 가져온 뒤 소유자를 비교하는 식으로 바꾸지 말 것. uuid 를 아는 것만으로 남의 이슈를 읽을 수 있게 된다.
- **설정(차단 언론사·위젯·날씨 위치)은 서버로 올리지 않는다.** 기기마다 다른 게 자연스럽다. 서버에 두는 것은 트래커뿐.
- **수집 창 분할은 4일에서 멈춘다(`MIN_SPLIT_DAYS`).** 인기 키워드는 어떤 창이든 응답이 가득 차서 "가득 차면 쪼갠다"만으로는 하루 단위까지 재귀한다. 실측: 24요청/60건/미완료 → 4일 하한 적용 후 15요청/94건/완료.
- **수동으로 꽂은 기사(`manual: true`)는 자동 중복 규칙보다 우선한다.** 같은 날 비슷한 제목이 있어도 남긴다 — 사용자가 그 기사를 직접 골랐기 때문이다. `pinToTracker()` 는 `dropped`·`skippedUrls` 에서도 url 을 빼야 한다(안 그러면 다음 수집이 도로 걷어낸다).
- **화면에 흔적이 남지 않는 동작에는 `toast()` 를 띄운다.** 기사를 이슈에 꽂으면 시트가 닫히고 뉴스 화면으로 돌아오는데 거기엔 아무 표시가 없어, 확인이 없으면 눌리지 않은 것처럼 보인다.
- **`openPickArticles()` 는 `newsData.categories` 전체를 뒤지되 차단 매체는 제외한다.** 숨긴 카테고리까지 뒤지는 이유는 기사가 타임라인에 없는 원인이 바로 평소 필터 밖이라서다. 반대로 차단한 언론사는 일부러 막은 것이니 수동 경로로 되살리지 않는다.
- **트래커 기본 동선은 버튼 하나(`↻ 업데이트`)다.** 최신 수집과 과거 소급을 `runUpdate()` 안에서 연달아 돌린다. 둘이 다른 경로로 온다는 건 앱 사정이라 사용자에게 나눠 보이지 않는다. 직접 추가·AI·수집 설정은 `⋯` 메뉴에 둔다.
- **`aiPrompt()` 문구를 임의로 줄이지 말 것.** 실패한 실제 세션을 근거로 세 가지(웹검색 강제 / 해외매체 대체 허용 / 링크 없는 항목 제외)를 명시한 것이다. 주석에 이유가 적혀 있다.
- **데모용 페이지(`_demo.html` 류)를 site/ 에 남기지 말 것.** localStorage 를 덮어쓰는 시드 페이지를 만들어 두면 주인님이 입력한 데이터가 날아간다 (2026-08-06 실제 사고).
- **입력 요소(`input`/`textarea`)를 16px 미만으로 만들지 말 것.** iOS Safari 가 포커스 시 페이지를 확대하고 되돌리지 않는다. 작게 보이고 싶으면 `@media (pointer: fine)` 안에서만 줄일 것. `maximum-scale=1` 로 막는 것은 금지(손가락 확대까지 막힘).
- **`openPanel()` 은 같은 패널을 다시 그릴 때 스크롤을 이어받는다** (`panel.dataset.title` 기준). 설정 토글마다 맨 위로 튕기던 버그를 막는 장치이니 제거하지 말 것.
- **설정을 바꾸는 핸들러는 `applySettingChange()` 를 쓴다.** `save()` + `openSettings()` 만 하면 패널 뒤의 대시보드·페이지가 갱신되지 않는다 (2026-08-05 실제 버그).
- **제목 번역은 화면에 보이는 페이지만 요청한다.** `rebuildPages()` 가 카테고리 페이지를 전부 미리 만들기 때문에, 문서 전체를 훑으면 해외 11개 페이지 165건이 한꺼번에 나간다(화면엔 15건). `onScreen()` 으로 걸러 화면 밖은 `data-tr` 을 남긴 채 건너뛰고, 스와이프가 멈추면 그 페이지를, 더보기를 누르면 새 항목만 번역한다.
- **번역이 막히면 엔드포인트가 아니라 요청 수를 줄인다.** `client=gtx` 와 `client=at` 은 **같은 per-IP 쿼터**를 공유한다 — client 를 갈아끼워도 한도는 그대로다(2026-08-31 실측: `at` 15연타에 둘 다 429). 배치(`&q=` 반복)·간격·백오프가 실질적인 수단이다.
- **번역은 Google 1순위 + MyMemory 폴백이다. 키가 필요한 제공자는 쓸 수 없다.** 정적 PWA 라 브라우저에 키가 노출되고, 서버를 거치면 Worker 요청 한도(50)에 걸린다. MS Translator·DeepL 을 붙이려면 이 두 가지를 먼저 풀어야 한다. Lingva 는 미러 6곳이 전부 죽어 있어 후보가 아니다(2026-08-31 실측).
- **MyMemory 는 한도 초과를 HTTP 가 아니라 본문 `responseStatus` 로 알린다.** `res.ok` 만 보면 못 잡는다. `quotaFinished` 도 함께 확인할 것. 익명 5천자/일 — 화면 4페이지분이라 주력이 아니라 폴백이다. `langpair` 는 `auto` 를 안 받아 `en|ko` 로 고정했다(해외 카테고리가 전부 영어라 가능).
- **번역 제공자의 실패는 `res.status` 로 판정할 수 없다.** 차단당한 Google 은 CORS 헤더 없는 HTML 차단 페이지를 주므로 브라우저가 응답을 막고 `fetch()` 가 "Failed to fetch" 로 거부한다 — **429 라는 상태코드는 페이지에서 볼 수 없다**(2026-08-31, `MissingAllowOriginHeader` 실측). `googleBatch` 의 **모든 실패**를 `unavailable()` 로 표시해 폴백시키고, 표시 없는 오류(진짜 코드 버그)만 위로 던진다.
- **번역 경로는 반드시 브라우저에서 검증할 것.** curl·Node·PowerShell 은 CORS 를 강제하지 않아 같은 요청이 성공한다. CLI 로만 확인하면 v1.0.3 처럼 "폴백을 붙였는데 실제로는 한 번도 호출되지 않는" 상태를 놓친다.
- **번역 실패 시 `data-tr` 을 지우지 말 것.** 표시를 먼저 지우고 요청하면 실패한 제목을 다음 sweep 이 두 번 다시 시도하지 않아 세션 내내 영어로 남는다(2026-08-31 실제 버그).
- **슬라이스가 안 도는 것처럼 보이면 최소 한 주기(20분)를 지켜본 뒤 판단한다.** 5분 간격 x 슬라이스 4개라, 실행이 한 번 누락되면 그 슬라이스는 다음 차례까지 20분을 기다린다 — 그 동안 "이 슬라이스만 죽었다"로 보인다. 2026-08-31 에 누락 1회를 패턴으로 읽고 "카테고리 추가가 슬라이스 1 을 깨뜨렸다"까지 결론냈으나, 다음 차례에 아무 조치 없이 정상 실행됐다. **로컬 측정(요청 수·용량·CPU·시간)이 전부 정상이면 고장이 아니라 관측을 의심할 것.**
- 기본 차단 목록(`DEFAULT_BLOCKED`)은 주인님이 지정한 것. 임의로 언론사를 추가·제거하지 말 것.

## TODO (마일스톤 순)

- [x] **M1 수집 파이프라인** (2026-08-05)
  - [x] repo 구조 생성 (`site/`, `scripts/`, `.github/workflows/`)
  - [x] `scripts/fetch-news.mjs`: Google 뉴스 RSS(카테고리별) 수집 → 정규화 → `site/data/news.json` (의존성 0, Node 내장 fetch)
  - [x] 제목 유사도 클러스터링 + 중요도 스코어 계산 (coverage × 최신성 반감기 12h)
  - [x] Actions workflow: 30분 cron + 수동 트리거, Pages 배포까지 (`collect.yml` — push 후 동작 검증 필요)
- [x] **M2 기본 뷰어 + 배포** (2026-08-05)
  - [x] `site/index.html`: 기사 리스트 (모바일 우선)
  - [x] GitHub `jayhouse2001/news_feed` (branch `master`), Pages Source = GitHub Actions
        → https://jayhouse2001.github.io/news_feed/ — iPhone 확인 완료
- [x] **M3 차단 기능** (2026-08-05)
  - [x] 언론사 차단 (기사 ⋯ 액션 시트 + 설정 탭 관리 화면, localStorage)
  - [x] 키워드 차단 (제목 부분일치, 대소문자 무시)
- [x] **M4 정렬** (2026-08-05)
  - [x] 카테고리별 정렬 기준 (최신순/중요도순/언론사 우선) — 카테고리마다 따로 기억
  - [x] 카테고리 on/off + 순서 변경 (설정 탭 △▽)
- [x] **M5 대시보드 (위젯 프레임)** (2026-08-05)
  - [x] 위젯 공통 구조: `WIDGET_TYPES` 레지스트리 + `widgetFrame()`, ⋯ 메뉴로 위로/아래로/삭제, ＋위젯 추가
  - [x] 중요 뉴스 위젯: 활성 카테고리 교차 스코어 상위 N 건(3/5/10 선택), 카테고리 배지, 유사기사 중복 제거(0.6)
  - [x] 바로가기 링크 위젯: 링크 추가/삭제/편집/순서변경 패널, URL scheme 그대로 href
- [x] **M6 날씨·미세먼지 위젯** (2026-08-05)
  - [x] 날씨: Open-Meteo forecast — 현재 기온·상태 + **7일 예보**(요일별 아이콘·최고/최저)
  - [x] 미세먼지: Open-Meteo air-quality (PM2.5 등급 칩 + PM10), 날씨와 한 위젯
  - [x] 위치: **geolocation 자동 취득**(8초 타임아웃) → 실패 시 저장된 위치 → 서울
- [x] **M7 PWA** (2026-08-05)
  - [x] manifest.json (standalone), icons(apple-touch 180 / 192 / 512 / maskable), iOS 메타 태그
- [x] **M8 확장** (2026-08-05)
  - [x] 해외 뉴스 8개 카테고리(US edition) + **제목 한국어 번역** (gtx, 키 불필요, localStorage 캐시, 실패 시 원문 + EN 배지)
  - [x] 언론사 차단 UI: 점선 칩 + 탭하면 취소선, 계열 묶음 차단/해제, 기본 차단 19곳(주인님 지정)
  - [x] 기사 열기 방식 선택: 현재 창 / 새 창 / 앱 안에서 읽기
- [x] **M9 하단 탭바 + 이슈 트래커 + 스크랩** (2026-08-06)
  - [x] 하단 탭바 4개(대시보드·뉴스·트래커·스크랩), ⚙ 는 상단 유지. `pagedots` 는 뉴스 탭에서만 표시
  - [x] 탭별 카테고리 위치 기억(`tabIndex`), `VIEW_KEY` 를 `{tab,index,scroll}` 로 확장 — 기사 열고 뒤로 와도 같은 탭 복귀
  - [x] 트래커: AND/OR 키워드 규칙 → 피드에서 자동 수집, 날짜별 타임라인 패널, 진행중/종료 세그먼트
  - [x] 기사 ⋯ 에서 "이슈에 추가"(수동 편입) / "스크랩에 저장"
  - [x] 트래커 위젯(대시보드), 탭 배지 = 마지막 열람 이후 새로 모인 건수
- [x] **M10 타임라인 개선 + 직접 기록 + GDELT 과거 소급** (2026-08-06)
  - [x] 타임라인을 날짜별 카드로, 세로 레일 진하게, 요일 표시, 5곳 이상 보도한 날 주황 강조
  - [x] 직접 기록(뉴스 링크 없는 사건) 추가/편집/삭제, 항목별 ✕ 삭제 (`dropped` 로 재수집 차단)
  - [x] GDELT 소급: 영어 키워드 + 시작일(기본 6개월) 입력 → 브라우저에서 직접 호출, 응답이 250건이면 창을 반씩 쪼개 재요청
  - [x] 소급 결과 축소: 주요 매체 화이트리스트 23곳(`MAJOR_DOMAINS`) + 하루 상한(3/5/8/15, 기본 8) + 제목 유사도 중복제거 → 248건 → 24건. 상한에 걸린 URL 은 `skippedUrls` 에 남겨 재실행 시 재추가 방지(상한을 올리면 초기화)
- [x] **M11 수동·AI 편집** (2026-08-06)
  - [x] Google 뉴스 검색 RSS 를 소급 1순위로 (한국어 검색 O, 연합·YTN 잡힘) → CORS 중계 4곳 동시 시도(`Promise.any`), 전부 실패 시 GDELT 로 대체
  - [x] 소급 패널에 시작일 직접 입력 + 빠른 선택(6개월/1년/2년/3년) — 이슈 편집에만 있어서 못 바꾸던 문제
  - [x] 증분 소급: `backfilledFrom`/`backfilledTo` 로 이미 가져온 범위 기억, 새로 열린 구간만 요청. `pendingSpans` 로 실패 구간 이어받기
  - [x] AI 타임라인 붙여넣기: `aiPrompt()` 복사 → AI 답변 붙여넣기 → `parseAiTimeline()` 파싱 → 미리보기 → 추가
  - [x] 모든 항목 추가/편집(✎)/삭제(✕). 링크 있는 기사도 편집 가능, 언론사 필드 추가
  - [x] 키워드 없는 이슈 허용 (AI·수동 전용). 카드에 "자동 수집 없음 · 직접 관리" 표시
- [ ] **남은 후보 (착수 전 주인님 확인)**
  - [ ] 소급 시작일 자동 탐지: `mode=timelinevol` 로 보도량 급증 지점을 찾아 시작일 제안 (현재는 사용자가 지정)
  - [ ] reader 모드 개선: Google 링크는 iframe 시도 없이 바로 Safari 로 (Google 이 프레임 삽입 거부 — 실측 확인됨)
  - [ ] 개별 언론사 RSS 소스 추가 (Actions 러너에서 접근 검증 필요)
  - [ ] 추가 위젯: 환율(Frankfurter ✅검증) / 관심 키워드 뉴스 / 나중에 읽기

## 확인된 사실 (재검증 불필요)

- Google 뉴스 RSS: `https://news.google.com/rss?hl=ko&gl=KR&ceid=KR:ko` (전체), 카테고리는 `https://news.google.com/rss/headlines/section/topic/<TOPIC>?hl=ko&gl=KR&ceid=KR:ko` (TOPIC: WORLD/NATION/BUSINESS/TECHNOLOGY/SCIENCE/ENTERTAINMENT/SPORTS/HEALTH). 항목에 `<source>` 태그로 언론사명+도메인 포함.
- TECHNOLOGY 와 SCIENCE 는 별개 피드이고 내용도 실제로 다름 (IT=기업·부품·가상자산, 과학=우주·생명과학·연구). 정치는 topic 피드가 없어 검색 RSS(`정치 when:1d`) 사용.
- Open-Meteo 날씨: 키 불필요·CORS 허용. 예) `https://api.open-meteo.com/v1/forecast?latitude=37.5665&longitude=126.978&current=temperature_2m,weather_code,precipitation&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max&timezone=Asia/Seoul`
- Open-Meteo 미세먼지: 키 불필요. 예) `https://air-quality-api.open-meteo.com/v1/air-quality?latitude=37.5665&longitude=126.978&current=pm10,pm2_5&timezone=Asia/Seoul`
- Frankfurter 환율: 키 불필요. 예) `https://api.frankfurter.dev/v1/latest?from=USD&to=KRW,JPY,EUR` (구 도메인 frankfurter.app 는 301 리다이렉트 — .dev 사용)
- **GDELT 2.0 Doc API** (`https://api.gdeltproject.org/api/v2/doc/doc`) — 키 불필요, **CORS 허용**(`Access-Control-Allow-Origin: *`), 브라우저 직접 호출 가능. 2026-08-06 실측:
  - **한국어 키워드 검색은 안 됨** (`대통령`, `이스라엘` → `{}` 0건). 2글자 이하는 "keyword too short" 거부. 한국어 토크나이징 미지원.
  - **영어 키워드 + `sourcecountry:southkorea sourcelang:korean` 조합이 정답** — 한국어 기사가 정상적으로 나옴. 예: `(iran OR iranian) sourcecountry:southkorea sourcelang:korean` → 2025-06 이란 충돌 기사 250건 전부 한국어 제목.
  - `maxrecords` **상한 250**. 2주 창도 큰 이슈면 꽉 참 → 응답이 250이면 창을 반으로 쪼개 재요청해야 유실이 없다(6개월 실측: 단순 월 단위 860건 vs 적응 분할 1436건).
  - `seendate` 형식은 `20260805T143000Z`. 응답이 JSON 이 아니면 에러 메시지(평문)다 — `{` 로 시작하는지 먼저 확인할 것.
  - **rate limit 이 매우 엄격**(5초당 1회 공지, 실제로는 누적되면 수 분간 429). 개발 중 연속 테스트하면 IP 가 막히니 주의. **빌드서버(B100039) IP 는 상시 429 — Windows 에서 테스트할 것.**
  - 본문 전체 매칭이라 무관한 기사가 섞임 → 가져온 뒤 한국어 키워드로 제목 재필터 필요.
  - **양이 과하다.** 실측(2025-06 이란 이슈 5일치): 250건 / 27개 도메인, 하루 최대 139건. 상위 5곳(파이낸셜뉴스·머니투데이·헤럴드경제·이데일리·뉴스핌)이 절반 이상을 차지하는 반면 **연합·YTN·SBS·MBC 는 거의 안 잡힌다.** → 주요 매체 화이트리스트 + 하루 상한이 필수 (248건 → 24건).
  - 제목 유사도 중복제거만으로는 거의 안 줄어든다(95 → 88). 매체가 달라 제목 표현이 제각각이라 유사도 0.6 에 안 걸림. **하루 상한이 실질적인 수단.**
- **Google 뉴스 검색 RSS 는 `after:`/`before:` 를 지원한다** — `https://news.google.com/rss/search?q=<키워드>+after:2025-06-10+before:2025-06-20&hl=ko&gl=KR&ceid=KR:ko`. 한국어 검색이 되고 연합뉴스·연합뉴스TV·BBC 등이 잡혀 GDELT 보다 품질이 낫다. 한 요청당 최대 100건. **단 CORS 헤더가 없어 브라우저에서 직접 못 읽는다.**
- **무료 CORS 중계는 신뢰할 수 없다** (2026-08-06 실측). 같은 시각에 allorigins/raw 는 실패하고 /get 은 성공, 몇 분 뒤엔 반대. 3회 반복 시 2회 실패. codetabs 521, corsproxy.io 403/503. → 여러 곳을 `Promise.any` 로 동시 시도하고, 전부 실패하면 GDELT 로 대체하는 구조가 필수. **소스(Google 뉴스) 자체는 서버에서 부르면 항상 200 이다** — 실패하면 중계를 의심할 것.
- 개별 언론사 사이트는 Claude 의 WebFetch 로는 차단됨 — 검증은 Actions 러너(또는 curl)에서 할 것.
- 번역: `https://translate.googleapis.com/translate_a/single?client=gtx&sl=auto&tl=ko&dt=t&q=...` — 키 불필요·CORS 허용. 비공식이라 막힐 수 있으니 실패 시 원문 유지가 필수. 작업 허브 `Z:\work1\work_note\widget_news.js` 가 같은 방식.
- **Google 뉴스 링크는 iframe 삽입 거부** ("news.google.com 연결을 거부했습니다") → 앱 내 reader 모드에서 대부분 안 열림. Safari 폴백 필수.
- 기사 링크는 Google 리다이렉트 URL 이지만 브라우저에서 열면 실제 언론사 본문으로 정상 이동함(실측). HTML 에는 원문 URL 이 없어 서버 측 추출 불가.
- 헤드리스 Edge 캡처는 우측이 잘려 보일 수 있음 — 레이아웃 문제로 오판하지 말고 `scrollWidth`/`clientWidth` 로 판단할 것.

## 검증 방법 (이 프로젝트에서 확립된 패턴)

`index.html` 을 복제해 `</body>` 앞에 진단 스크립트를 삽입한 임시 페이지(`_probe*.html`)를 만들고 헤드리스 Edge 로 캡처해 DOM 상태를 수치로 확인한다. iframe 방식은 링크 클릭이 프레임을 이동시켜 실패하므로 **앱과 같은 문서에서** 실행할 것. 검증 후 임시 파일은 삭제.
